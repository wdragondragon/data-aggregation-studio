# 字段映射规则模块 Web 冒烟结果

## 1. 结论

结论：通过。

本轮确认 Web 端字段映射规则页面在迁移后可正常访问，内置规则已按初始化数据成功展示。

## 2. 执行环境

| 项目 | 信息 |
| --- | --- |
| 执行日期 | 2026-04-21 |
| 前端 | `http://127.0.0.1:5173` |
| Studio Server | `http://127.0.0.1:18080` |
| Studio Worker | `http://127.0.0.1:18081` |
| 数据库 | SQLite：`backend/studio-test/target/test-runtime/web-smoke.db` |
| 登录账号 | `admin / admin123` |
| 当前租户 / 项目 | `Default Tenant / Default Project` |

## 3. 执行结果

| 用例 | 验证点 | 结果 |
| --- | --- | --- |
| FMR-UI-001 | `/field-mapping-rules` 页面正常进入，标题与上下文显示正确 | 通过 |
| FMR-UI-002 | 列表成功加载内置规则数据 | 通过 |
| FMR-UI-003 | 首屏显示总数 `26`，分页默认为 `20条/页`，可见下一页按钮 | 通过 |
| FMR-UI-004 | 关键字、映射类型、启用状态筛选区完整展示 | 通过 |
| FMR-UI-005 | “新建规则”“编辑”“删除”“刷新”按钮均可见 | 通过 |
| FMR-UI-006 | 浏览器控制台未发现 `error` / `warn` | 通过 |

## 4. 现场记录

首屏已加载的规则示例：

| 序号 | 映射名 | 映射类型 | 映射编码 |
| --- | --- | --- | --- |
| 1 | `aes加密` | `加密` | `aes_str` |
| 2 | `des加密` | `加密` | `des_str` |
| 3 | `3des加密` | `加密` | `3des_str` |
| 11 | `保留小数点后n位` | `规整` | `number_cut` |

## 5. 本轮未覆盖

以下能力本轮未继续深测：

| 项目 | 说明 |
| --- | --- |
| 新建规则保存 | 本轮只验证入口与列表加载，未在 Web 端提交新增规则 |
| 编辑、删除 | 本轮未修改内置规则，避免污染初始化样例数据 |
| 条件筛选提交 | 本轮确认筛选控件已渲染，未逐项提交筛选条件 |

## 6. 说明

由于内置规则由初始化过程自动注入，本轮重点确认的是“规则注册表首屏是否正常工作”，以验证 Boot3 迁移后的前后端契约未破坏。
