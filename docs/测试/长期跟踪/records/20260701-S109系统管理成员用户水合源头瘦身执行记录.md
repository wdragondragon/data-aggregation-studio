# 2026-07-01 S109 系统管理成员用户水合源头瘦身执行记录

## 触发任务

延续 Studio 长期测试目标，对系统管理成员与申请列表继续结合代码深挖隐藏过度读取。用户补充边界：不用把优化做到牺牲正常列表体验，分页该拉取仍拉取，状态该刷新仍刷新；本轮只处理表格水合用户信息时读取明显无关宽字段/敏感字段的问题。

## 长期约定

- Studio 入口：`http://127.0.0.1:8000/dfs/data-aggregation-studio`
- 默认账号：`admin / admin123`
- 长期项目：`长期回归测试项目` / `2068077680446365698`
- 长期库：`studio_longterm_regression`
- Nacos：`127.0.0.1:8848`
- 长期测试数据不清理。

## 本轮长期数据

本轮未新增、未删除长期数据，复用系统管理长期用户、项目成员与申请记录：

| 类型 | 对象 |
|---|---|
| 当前项目 | `长期回归测试项目` / `2068077680446365698` |
| 项目成员样例 | `长期回归-客户运营项目成员`、`长期回归-成员复活路径用户`、`长期回归-M09权限接口实服普通成员` 等 |
| 申请/邀请样例 | `长期回归S84访问中心写后刷新收敛验证` 等历史申请记录 |

## 排查范围

| 候选点 | 结论 |
|---|---|
| `/system/project-members/page` | 真实存在问题：项目成员分页只展示 `username/displayName` 等轻量用户信息，但内部 `loadUserMap()` 使用 `selectByIds` 读取用户整行 |
| `/system/project-member-requests/page` | 真实存在问题：申请/邀请分页为申请人、邀请人、审批人水合用户名时同样读取用户整行 |
| `/system/tenant-members/page` | 受同一 `loadUserMap()` 影响，修复后同步受益 |
| `/users`、`/users/page`、`/users/options` | 保留原接口语义；本轮不改变用户表格列表和用户下拉 |

## 问题确认

| 编号 | 模块 | 现象 | 真实存在 | 是否需要修复 |
|---|---|---|---|---|
| S109-SYS-001 | 系统管理 / 成员列表 | 成员、申请/邀请表格只需用户 `id/username/displayName`，但 `loadUserMap()` 通过 `selectByIds` 读取 `sys_user` 整行，包含 `passwordHash/authSource/enabled/tenant/deleted/timestamps` 等非表格字段 | 是 | 是 |

## 修复范围

| 文件 | 变更 |
|---|---|
| `backend/studio-infra/src/main/java/com/jdragon/studio/infra/service/SystemManagementService.java` | `loadUserMap()` 改为 `LambdaQueryWrapper.select(id, username, displayName).in(id, userIds)`，源头只读取表格水合需要字段 |
| `backend/studio-test/src/test/java/com/jdragon/studio/test/SystemManagementPaginationSourceSlimmingRegressionTest.java` | 补项目成员与项目成员申请分页回归断言，校验不再调用 `selectByIds`，且 SQL select 不包含密码、认证来源、租户、删除标记和时间戳字段 |

## 验证结果

| 用例 | 入口 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| S109-BE-001 | `SystemManagementPaginationSourceSlimmingRegressionTest` | 成员与申请分页保留当前页分页读取，用户水合仅选 `id/username/display_name` | 7 tests / 0 failures / 0 errors，Maven `BUILD SUCCESS` | PASS |
| S109-PKG-001 | `mvn -pl studio-server -am -DskipTests package` | Server exec jar 重新打包成功 | Reactor 7/7 SUCCESS，生成 `studio-server-0.1.0-SNAPSHOT-exec.jar` | PASS |
| S109-RUN-001 | 重启 `StudioServerApplication` 等价 Server | 新代码加载，Nacos 指向 `127.0.0.1:8848` | 新 PID `21544` 监听 `18080`，业务 API 可用 | PASS |
| S109-API-001 | build-nginx API `/system/project-members/page?projectId=2068077680446365698&pageNo=1&pageSize=5` | 返回项目成员当前页，不包含密码、认证来源等敏感字段 | `SUCCESS`，`total=5`，首行字段为表格字段，无 `password/authSource/externalAccount` | PASS |
| S109-API-002 | build-nginx API `/system/project-member-requests/page?projectId=2068077680446365698&pageNo=1&pageSize=5` | 返回申请/邀请当前页，不包含密码、认证来源等敏感字段 | `SUCCESS`，`total=2`，首行字段为申请表格字段，无 `password/authSource/externalAccount` | PASS |
| S109-UI-001 | build-nginx `/system?tab=projectMembers` | 项目成员页正常显示当前页，console 无 warn/error | 页面显示 5 条项目成员，分页 `共 5 条`，console warn/error `0` | PASS |
| S109-UI-002 | build-nginx `/system?tab=requests` | 申请/邀请页正常显示当前页，console 无 warn/error | 页面显示 2 条申请/邀请，分页 `共 2 条`，console warn/error `0` | PASS |

执行命令摘要：

```powershell
cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\backend
$env:JAVA_HOME='C:\dev\Java\jdk-17.0.12'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
mvn -pl studio-test -am '-Dtest=SystemManagementPaginationSourceSlimmingRegressionTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
mvn -pl studio-server -am '-DskipTests' package
```

## 观察项

- 本轮没有修改前端代码，因此未重建前端；页面验证使用 build-nginx 已构建入口。
- `/system/project-members/page` 和 `/system/project-member-requests/page` 仍按当前页分页读取，这是正常表格行为，不作为问题。
- 本轮修复的是表格结果二次水合用户名称时的源头字段范围，不改变分页、筛选、刷新或表格返回结构。

## 结论

- 真实问题已修复。
- 系统管理成员、租户成员、项目成员申请/邀请的用户水合不再读取用户整行，改为只查询 `id/username/displayName`。
- 后续涉及 `SystemManagementService.loadUserMap()`、系统管理成员分页、申请/邀请分页或用户敏感字段暴露时，至少复跑 `SystemManagementPaginationSourceSlimmingRegressionTest`、server package、build-nginx API 两个分页接口和 build-nginx `/system?tab=projectMembers`、`/system?tab=requests`。
