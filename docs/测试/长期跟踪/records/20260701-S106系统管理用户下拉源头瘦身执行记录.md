# 2026-07-01 S106 系统管理用户下拉源头瘦身执行记录

## 触发任务

延续 Studio 长期测试目标，对系统管理当前页继续结合代码二次验证。用户边界继续执行：分页表格和当前页状态刷新允许正常拉取；本轮只处理弹窗下拉复用表格/全量列表接口导致的隐藏字段过取。

## 长期约定

- Studio 入口：`http://127.0.0.1:8000/dfs/data-aggregation-studio`
- 默认账号：`admin / admin123`
- 长期项目：`长期回归测试项目` / `2068077680446365698`
- 长期库：`studio_longterm_regression`
- Nacos：`127.0.0.1:8848`
- 长期测试数据不清理。

## 本轮长期数据

本轮未新增长期测试数据，复用已有长期用户：

| 类型 | 对象 |
|---|---|
| 管理员 | `admin` / `Studio Admin` |
| 长期项目成员 | `lt_reg_customer_ops_member` / `长期回归-客户运营项目成员` |
| 长期项目成员 | `lt_reg_s01_long_project_member` / `长期回归-S01客户运营普通成员` |

## 排查范围

| 候选点 | 结论 |
|---|---|
| `/system?tab=users` 用户表格 | 属于表格分页/列表视图，已由 S28/S64 覆盖，保留现有字段 |
| `/system?tab=projectMembers` 表格 | 属于当前页分页拉取，保留 |
| 租户成员、项目成员、申请/邀请弹窗用户下拉 | 真实存在问题：下拉只需要 `id/username/displayName`，却调用 `/users` 全量列表接口，响应包含 `tenantId/deleted/createdAt/updatedAt/enabled` |

## 问题确认

| 编号 | 模块 | 现象 | 真实存在 | 是否需要修复 |
|---|---|---|---|---|
| S106-SYS-001 | 系统管理 / 用户下拉 | 三个用户选择弹窗复用 `studioApi.users.list()`，该接口面向用户列表视图，返回表格字段；弹窗只使用 `id/username/displayName` | 是 | 是 |

修复前 API 证据：

- build-nginx `GET /api/v1/users` 返回 18 条。
- 首条字段为 `id,tenantId,deleted,createdAt,updatedAt,username,displayName,enabled`。
- 弹窗 `userLabel()` 只使用 `displayName/username`，保存动作只使用 `userId`。

## 修复范围

| 文件 | 变更 |
|---|---|
| `backend/studio-dto/src/main/java/com/jdragon/studio/dto/model/StudioUserOptionView.java` | 新增用户下拉轻量 DTO |
| `backend/studio-infra/src/main/java/com/jdragon/studio/infra/service/UserManagementService.java` | 新增 `listOptions()`，源查询只选择 `id/username/displayName` |
| `backend/studio-server/src/main/java/com/jdragon/studio/server/web/controller/UserController.java` | 新增 `GET /users/options` |
| `frontend/packages/api-sdk/src/types.ts`、`frontend/packages/api-sdk/src/client.ts` | 新增 `StudioUserOption` 和 `users.options()` |
| `frontend/apps/web/src/views/SystemView.vue` | 租户成员、项目成员、申请/邀请弹窗用户下拉改用轻量选项接口 |
| `backend/studio-test/src/test/java/com/jdragon/studio/test/SystemManagementPaginationSourceSlimmingRegressionTest.java` | 新增用户选项源头字段断言 |

## 验证结果

| 用例 | 入口 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| S106-BE-001 | `SystemManagementPaginationSourceSlimmingRegressionTest` | 用户分页、用户选项和系统管理分页源头瘦身回归通过 | 5 tests / 0 failures / 0 errors，Maven `BUILD SUCCESS` | PASS |
| S106-FE-001 | `npm run build:web` | API SDK 和系统页类型构建通过 | Vite 构建成功，`SystemView-*.js` 产物生成 | PASS |
| S106-PKG-001 | `mvn -pl studio-server -am -DskipTests package` | Server exec jar 重新打包成功 | Reactor 7/7 SUCCESS，生成 `studio-server-0.1.0-SNAPSHOT-exec.jar` | PASS |
| S106-RUN-001 | 重启 `StudioServerApplication` 等价 Server | 新接口加载，Nacos 指向 `127.0.0.1:8848` | 新 PID `13016` 监听 `18080`，业务 API 可用 | PASS |
| S106-API-001 | build-nginx `GET /api/v1/users/options` | 返回用户下拉字段，不含表格字段 | `SUCCESS`，返回 18 条；首条 `admin / Studio Admin`；响应字段仅 `id/username/displayName`，不含 `tenantId/deleted/createdAt/updatedAt/enabled/password` | PASS |
| S106-API-002 | build-nginx `GET /api/v1/users/page?pageNo=1&pageSize=5` | 用户表格分页仍正常 | `SUCCESS`，`total=18`，表格字段仍包含 `tenantId/deleted/createdAt/updatedAt/enabled` | PASS |
| S106-UI-001 | build-nginx `/system?tab=projectMembers` | 项目成员弹窗用户下拉可用，console 无 warn/error | 打开“新增项目成员”，用户下拉可见 `Studio Admin (admin)` 和中文长期用户选项，console warn/error `0` | PASS |

执行命令摘要：

```powershell
cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\backend
$env:JAVA_HOME='C:\dev\Java\jdk-17.0.12'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
mvn -pl studio-test -am '-Dtest=SystemManagementPaginationSourceSlimmingRegressionTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
mvn -pl studio-server -am '-DskipTests' package

cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\frontend
npm run build:web
```

## 观察项

- `/users` 和 `/users/page` 保留原列表视图语义，避免影响用户表格和现有兼容。
- 新 `/users/options` 当前仍返回全部用户选项，但源字段已收敛到下拉所需字段；后续若用户量继续增大，可进一步做远程搜索分页下拉。
- 本轮未做写入动作，未新增或清理长期数据。

## 结论

- 真实问题已修复。
- 系统管理三个用户选择弹窗不再复用用户表格列表接口，改为源头轻量的 `/users/options`。
- 后续涉及 `SystemView.vue`、`UserManagementService`、用户列表、租户成员/项目成员/申请邀请弹窗时，至少复跑 `SystemManagementPaginationSourceSlimmingRegressionTest`、`npm run build:web`、build-nginx API `/users/options`、`/users/page` 和 build-nginx `/system?tab=projectMembers`。
