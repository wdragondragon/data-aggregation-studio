# 2026-07-01 S108 系统管理资源共享保存校验源头瘦身执行记录

## 触发任务

延续 Studio 长期测试目标，对系统管理资源共享页继续结合代码深挖隐藏过度读取。用户边界继续执行：分页列表和当前页状态刷新允许正常拉取；本轮只处理保存资源共享时的后端校验/通知标签解析读取完整资源实体问题。

## 长期约定

- Studio 入口：`http://127.0.0.1:8000/dfs/data-aggregation-studio`
- 默认账号：`admin / admin123`
- 长期项目：`长期回归测试项目` / `2068077680446365698`
- 长期库：`studio_longterm_regression`
- Nacos：`127.0.0.1:8848`
- 长期测试数据不清理。

## 本轮长期数据

本轮复用已有数据服务，并新增/保留资源共享记录：

| 类型 | 对象 |
|---|---|
| 来源项目 | `长期回归测试项目` / `2068077680446365698` |
| 目标项目 | `长期回归-资源接收项目` / `2068301893337849857` |
| 数据服务资源 | `长期回归-S10合同结构扰动查询服务 (lt_reg_s10_contract_schema_service_api)` / `2068704693544779778` |
| 新增共享 | `2072094621460852738`，`DATA_SERVICE`，共享到 `长期回归-资源接收项目` |
| 观察共享 | `2072094476396654593`，`DATA_SERVICE`，共享到 `Default Project`；PowerShell 中文匹配失败产生，按长期追踪约定保留不清理 |

## 排查范围

| 候选点 | 结论 |
|---|---|
| `/system/resource-share-options` 资源候选下拉 | 已是源头轻量查询，各类型只选择下拉需要字段 |
| `/system/resource-shares/page` 资源共享分页 | 已由 S67 修复为当前页标签水合，保留正常分页 |
| `POST /system/resource-shares` 保存校验 | 真实存在问题：`validateShareableResource()` 使用各资源 mapper 的 `selectById` 读取完整实体，只需要租户/项目归属校验 |
| 资源共享通知标签 | 真实存在问题：`resolveSharedResourceLabel()` 使用 `selectById` 读取完整资源实体，只需要资源名称 |

## 问题确认

| 编号 | 模块 | 现象 | 真实存在 | 是否需要修复 |
|---|---|---|---|---|
| S108-SYS-001 | 系统管理 / 资源共享保存 | 保存数据服务、数据接入、协议转换、工作流、脚本等共享时，校验资源归属通过 `selectById` 读取完整定义实体，可能包含 `customSql`、字段映射、转换器、WebService 配置等大字段 | 是 | 是 |
| S108-SYS-002 | 系统管理 / 资源共享通知 | 启用共享后构造通知内容时再次 `selectById` 读取完整资源实体，只为得到展示名称 | 是 | 是 |

## 修复范围

| 文件 | 变更 |
|---|---|
| `backend/studio-infra/src/main/java/com/jdragon/studio/infra/service/SystemResourceShareSupport.java` | `validateShareableResource()` 改为复用源头轻量的 `ShareResourceOptionView` 查询；`resolveSharedResourceLabel()` 改为轻量查询取名称，不再 `selectById` 整实体 |
| `backend/studio-test/src/test/java/com/jdragon/studio/test/ResourceShareServiceTypeRegressionTest.java` | 新增源头字段断言，覆盖数据服务、数据接入、协议转换服务保存与通知标签解析，断言不读取大字段且不调用 `selectById` |

## 验证结果

| 用例 | 入口 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| S108-BE-001 | `ResourceShareServiceTypeRegressionTest` | 保存三类开放资源共享不调用 `selectById`，只查询轻量字段 | 4 tests / 0 failures / 0 errors，Maven `BUILD SUCCESS` | PASS |
| S108-BE-002 | `SystemManagementPaginationSourceSlimmingRegressionTest,ResourceShareServiceTypeRegressionTest` | 资源共享表格/下拉既有源头瘦身不回退 | 10 tests / 0 failures / 0 errors，Maven `BUILD SUCCESS` | PASS |
| S108-PKG-001 | `mvn -pl studio-server -am -DskipTests package` | Server exec jar 重新打包成功 | Reactor 7/7 SUCCESS，生成 `studio-server-0.1.0-SNAPSHOT-exec.jar` | PASS |
| S108-RUN-001 | 重启 `StudioServerApplication` 等价 Server | 新保存链路加载，Nacos 指向 `127.0.0.1:8848` | 新 PID `12708` 监听 `18080`，业务 API 可用 | PASS |
| S108-API-001 | build-nginx `POST /api/v1/system/resource-shares` | 保存数据服务共享成功，走新轻量校验路径 | `SUCCESS`，保存 `DATA_SERVICE:2068704693544779778` 到 `长期回归-资源接收项目`，返回共享 `2072094621460852738` | PASS |
| S108-UI-001 | build-nginx `/system?tab=shares` | 资源共享页切换到“数据服务”后显示新增长期共享，console 无 warn/error | 页面显示 `长期回归-S10合同结构扰动查询服务 (lt_reg_s10_contract_schema_service_api)`，目标项目包含 `长期回归-资源接收项目`，console warn/error `0` | PASS |

执行命令摘要：

```powershell
cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\backend
$env:JAVA_HOME='C:\dev\Java\jdk-17.0.12'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
mvn -pl studio-test -am '-Dtest=ResourceShareServiceTypeRegressionTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
mvn -pl studio-test -am '-Dtest=SystemManagementPaginationSourceSlimmingRegressionTest,ResourceShareServiceTypeRegressionTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
mvn -pl studio-server -am '-DskipTests' package
```

## 观察项

- 本轮没有修改前端代码，因此未重建前端；页面验证仍使用 build-nginx 已构建入口。
- `POST /system/resource-shares` 仍会保存后返回 `ResourceShareEntity`，该实体自身字段较小，前端随后按当前资源类型分页刷新，符合“分页状态可刷新”的边界。
- PowerShell 控制台中文编码导致第一次 API 验证未匹配到 `长期回归-资源接收项目`，创建了共享到 `Default Project` 的长期记录；随后用 Python UTF-8 客户端补充正确目标项目验证。两条记录均保留，用于后续可追溯复测。

## 结论

- 真实问题已修复。
- 资源共享保存校验和通知标签解析不再读取完整资源定义，改为源头轻量字段查询。
- 后续涉及 `SystemResourceShareSupport`、资源共享保存、资源共享通知、开放服务定义大字段或资源共享页面时，至少复跑 `ResourceShareServiceTypeRegressionTest`、`SystemManagementPaginationSourceSlimmingRegressionTest`、server package、build-nginx API `POST /system/resource-shares` 和 build-nginx `/system?tab=shares`。
