# 2026-07-01 S110 系统管理资源共享项目名称水合源头瘦身执行记录

## 触发任务

延续 Studio 长期测试目标，对系统管理资源共享表格继续结合代码深挖隐藏过度读取。用户边界保持不变：分页列表该拉取仍拉取，状态该刷新仍刷新；本轮只处理表格字段水合时读取无关字段的问题。

## 长期约定

- Studio 入口：`http://127.0.0.1:8000/dfs/data-aggregation-studio`
- 默认账号：`admin / admin123`
- 长期项目：`长期回归测试项目` / `2068077680446365698`
- 长期库：`studio_longterm_regression`
- Nacos：`127.0.0.1:8848`
- 长期测试数据不清理。

## 本轮长期数据

本轮未新增、未删除长期数据，复用已有资源共享记录：

| 类型 | 对象 |
|---|---|
| 当前项目 | `长期回归测试项目` / `2068077680446365698` |
| 资源共享样例 | `长期回归-Studio公开健康检查HTTP数据源 (http)` 共享到 `长期回归-资源接收项目` |
| 数据服务共享样例 | `长期回归-S10合同结构扰动查询服务` / `2068704693544779778` 共享到 `长期回归-资源接收项目` |

## 排查范围

| 候选点 | 结论 |
|---|---|
| `/system/resource-shares/page` 资源共享分页 | 真实存在问题：表格只展示源项目名和目标项目名，但 `loadProjectsForShares()` 通过 `projectMapper.selectByIds` 读取项目整行 |
| 资源共享资源标签水合 | 已由 S67/S108 覆盖，继续走 `ShareResourceOptionView` 轻量查询 |
| `/system/projects`、`/system/projects/page` | 保留项目管理列表语义，本轮不改变 |
| `/system/project-options` | 已是项目下拉轻量接口，本轮不改变 |

## 问题确认

| 编号 | 模块 | 现象 | 真实存在 | 是否需要修复 |
|---|---|---|---|---|
| S110-SYS-001 | 系统管理 / 资源共享表格 | 资源共享表格只需项目 `id/projectName` 用于源项目和目标项目展示，但内部通过 `selectByIds` 读取项目整行，包含项目编码、描述、启用状态、默认项目标记、租户和时间戳等非表格水合字段 | 是 | 是 |

## 修复范围

| 文件 | 变更 |
|---|---|
| `backend/studio-infra/src/main/java/com/jdragon/studio/infra/service/SystemManagementService.java` | `loadProjectsForShares()` 改为 `LambdaQueryWrapper.select(id, projectName).in(id, projectIds)`，源头只读取资源共享表格水合需要字段 |
| `backend/studio-test/src/test/java/com/jdragon/studio/test/SystemManagementPaginationSourceSlimmingRegressionTest.java` | 在资源共享分页回归中补项目名称水合字段断言，校验不再调用 `selectByIds`，且 SQL select 不包含项目编码、描述、启用状态、默认项目标记、租户和时间戳字段 |

## 验证结果

| 用例 | 入口 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| S110-BE-001 | `SystemManagementPaginationSourceSlimmingRegressionTest` | 资源共享分页保留当前页分页读取，项目名称水合仅选 `id/project_name` | 7 tests / 0 failures / 0 errors，Maven `BUILD SUCCESS` | PASS |
| S110-PKG-001 | `mvn -pl studio-server -am -DskipTests package` | Server exec jar 重新打包成功 | Reactor 7/7 SUCCESS，生成 `studio-server-0.1.0-SNAPSHOT-exec.jar` | PASS |
| S110-RUN-001 | 重启 `StudioServerApplication` 等价 Server | 新代码加载，Nacos 指向 `127.0.0.1:8848` | 新 PID `29940` 监听 `18080`，业务 API 可用 | PASS |
| S110-API-001 | build-nginx API `/system/resource-shares/page?resourceType=DATA_SERVICE&pageNo=1&pageSize=5` | 数据服务资源共享分页正常返回源/目标项目名称 | `SUCCESS`，`total=2`，返回字段不包含 `projectCode/description/defaultProject` | PASS |
| S110-UI-001 | build-nginx `/system?tab=shares` | 资源共享页正常显示当前页，console 无 warn/error | 页面显示 `长期回归-Studio公开健康检查HTTP数据源 (http)` 共享到 `长期回归-资源接收项目`，分页 `共 1 条`，console warn/error `0` | PASS |

执行命令摘要：

```powershell
cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\backend
$env:JAVA_HOME='C:\dev\Java\jdk-17.0.12'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
mvn -pl studio-test -am '-Dtest=SystemManagementPaginationSourceSlimmingRegressionTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
mvn -pl studio-server -am '-DskipTests' package
```

## 观察项

- 本轮没有修改前端代码，因此未重建前端；页面验证使用 build-nginx 已构建入口。
- `/system/resource-shares/page` 仍按当前页分页读取 `ResourceShareEntity`，符合正常表格分页行为。
- 本轮修复的是资源共享表格结果二次水合项目名称时的源头字段范围，不改变分页、筛选、刷新或表格返回结构。

## 结论

- 真实问题已修复。
- 资源共享表格源项目/目标项目名称水合不再读取项目整行，改为只查询 `id/projectName`。
- 后续涉及 `SystemManagementService.loadProjectsForShares()`、资源共享分页、项目字段水合或系统管理资源共享页时，至少复跑 `SystemManagementPaginationSourceSlimmingRegressionTest`、server package、build-nginx API `/system/resource-shares/page` 和 build-nginx `/system?tab=shares`。
