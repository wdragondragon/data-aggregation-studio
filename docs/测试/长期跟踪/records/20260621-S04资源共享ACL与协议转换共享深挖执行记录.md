# S04 资源共享 ACL 与协议转换共享深挖执行记录

## 基本信息

| 项 | 值 |
|---|---|
| 测试日期 | 2026-06-21 |
| 测试入口 | `http://127.0.0.1:8000/dfs/data-aggregation-studio/system` |
| API 入口 | `http://127.0.0.1:18080/api/v1` |
| 长期项目 | `长期回归测试项目` / `2068077680446365698` |
| 接收项目 | `长期回归-资源接收项目` / `2068301893337849857` |
| 长期库 | `studio_longterm_regression`，本轮未清理、不删除历史数据 |
| 测试账号 | `admin / admin123`；接收项目成员 `lt_reg_s01_receiver_member / LtRegS01!2026` |
| 运行环境 | IDEA `StudioServerApplication`、`StudioWorkerApplication`，`NACOS_SERVER=127.0.0.1:8848` |
| Python | `C:\dev\python-envs\studio-longterm-conda311\python.exe` |

## 已排除范围

- 不重复全覆盖路由点通、菜单点通、基础保存/发布/下线/删除流程。
- 不重复 M00-M10、S01、S02、S03 首轮人工点测。
- S01 缺少 `X-Project-Id` 的 API 探针本轮已做代码/API假设验证，未发现项目隔离绕过；未作为新缺陷记录。
- 已进入缺陷索引的 M03-M10、S03 修复项仅作为本轮回归门槛参考。

## 本轮新增或保留数据

| 类型 | 标识 | 说明 |
|---|---|---|
| 协议转换服务 | `2068145027555180546` / `长期回归-Studio健康检查协议转换` / `lt_reg_health_protocol_api` | 复用 M05 保留的长期协议转换服务 |
| 资源共享 | `2068643969963360257` | 本轮修复后创建并保留，类型 `PROTOCOL_CONVERSION_SERVICE`，从长期项目共享到接收项目 |

## 环境健康

| 检查项 | 结果 | 摘要 |
|---|---|---|
| Nacos readiness | PASS | `GET /nacos/v1/console/health/readiness` 返回 `200 OK` / `OK` |
| Server health | PASS | 重启加载修复后 `GET /actuator/health` 返回 `200` / `UP` |
| Worker health | PASS | 重启加载修复后 `GET /actuator/health` 返回 `200` / `UP` |

备注：重启前 18080/18081 仍是旧运行进程，health 曾返回 `DOWN`，且协议转换共享仍返回修复前的 400。已通过 IDEA run configuration 重启 Server/Worker 后复测通过；该现象判定为运行态未加载新代码，不计入产品缺陷。

## 测试用例与结果

| 编号 | 模块 | 前置数据 | 步骤 | 预期 | 实际 | 结果 | 是否需要修复 |
|---|---|---|---|---|---|---|---|
| S04-ACL-001 | 系统管理/资源共享 | 长期项目 admin token | 缺省/错误项目上下文访问系统共享接口并结合代码检查 `requireManageableProject`、`ProjectResourceAccessService` | 不应绕过当前项目管理权限 | 未发现项目隔离绕过；本轮不扩展为缺陷 | PASS | 否 |
| S04-SHARE-001 | 系统管理/资源共享 | 协议转换服务 `2068145027555180546` | `POST /system/resource-shares`，`resourceType=PROTOCOL_CONVERSION_SERVICE` | 协议转换服务应与数据源、模型、采集、工作流、脚本、数据服务、数据接入服务一样可共享 | 修复前实服返回 `400 BAD_REQUEST Unsupported resource type for sharing: PROTOCOL_CONVERSION_SERVICE` | FAIL | 是 |
| S04-SHARE-002 | 代码二次验证 | 当前源码 | 检查 `ProtocolConversionService`、`StudioConstants`、`SystemResourceShareSupport`、前端 `systemViewSupport.ts` 和 `SystemView.vue` | 后端常量、共享校验、通知跳转、前端枚举和候选资源加载应一致 | 协议转换服务已有共享资源类型常量和服务侧共享访问能力，但系统共享后端校验/通知/跳转及前端枚举/候选加载缺失 | FAIL | 是 |
| S04-FIX-001 | 后端修复 | 当前源码 | 为 `SystemResourceShareSupport` 接入 `ProtocolConversionServiceMapper`，补充校验、资源展示名、跳转路径，并更新 `SystemManagementService` 构造 | 后端接受协议转换服务共享，并能正确生成共享通知目标 | 后端单测通过，实服 POST 返回 `200 SUCCESS` | PASS | 已修复 |
| S04-FIX-002 | 前端修复 | 当前源码 | 在系统资源共享枚举加入“协议转换服务”，加载协议转换服务列表并用于资源候选项 | `/system` 共享弹窗可选择协议转换服务及长期协议转换服务 | 浏览器验证下拉包含“协议转换服务”，资源候选包含“长期回归-Studio健康检查协议转换 (lt_reg_health_protocol_api)” | PASS | 已修复 |
| S04-REG-001 | 接收项目可见性 | 共享记录 `2068643969963360257` | 接收项目成员登录并查询 `/protocol-conversions` | 接收项目应能看到共享协议转换服务 | 返回 `200 SUCCESS`，列表包含 `2068145027555180546` | PASS | 否 |
| S04-REG-002 | 浏览器控制台 | `/system` 资源共享弹窗 | 打开页签、弹窗、下拉并检查控制台 | 无 Studio 相关 error/warn | Browser logs `[]` | PASS | 否 |

## 修复摘要

- 后端：
  - `SystemResourceShareSupport` 增加协议转换服务资源校验、共享通知标签和目标路径 `/protocol-conversions`。
  - `SystemManagementService` 构造注入 `ProtocolConversionServiceMapper` 并传入共享支持类。
- 前端：
  - `systemViewSupport.ts` 增加 `PROTOCOL_CONVERSION_SERVICE` 资源类型选项。
  - `SystemView.vue` 加载协议转换服务资源，并在资源共享表单中按当前项目筛选候选项。
- 测试：
  - `ResourceShareServiceTypeRegressionTest` 新增 `shouldShareProtocolConversionService`。
  - 受构造签名影响的系统管理回归测试补充 `ProtocolConversionServiceMapper` mock。

## 验证命令与结果

| 验证 | 命令/方式 | 结果 |
|---|---|---|
| 后端回归测试 | `JAVA_HOME=C:\dev\Java\jdk-17.0.12; mvn -pl studio-test -am "-Dtest=ResourceShareServiceTypeRegressionTest,SystemProjectWorkerViewRegressionTest,SystemProjectMemberRequestRegressionTest" "-Dsurefire.failIfNoSpecifiedTests=false" "-DforkCount=0" test` | PASS，`Tests run: 8, Failures: 0, Errors: 0, Skipped: 0` |
| 前端构建 | `npm run build:web` | PASS，Vite build success；仅有既有 npm config 与 Rollup 注释 warning |
| 实服 API | inline Python probe，登录 admin 后创建协议转换共享 | PASS，`sharePost.status=200`，共享记录 `2068643969963360257` |
| 接收项目回归 | inline Python probe，登录 `lt_reg_s01_receiver_member` 查询 `/protocol-conversions` | PASS，列表包含 `长期回归-Studio健康检查协议转换` |
| UI 回归 | Browser 打开 `/system`，资源共享页签，打开“共享资源”弹窗 | PASS，下拉和资源候选项正确，控制台无 error/warn |
| S01 共享权限回归 | `C:\dev\python-envs\studio-longterm-conda311\python.exe docs\测试\长期跟踪\scripts\studio_s01_permission_probe.py` | PASS，`25 PASS / 0 FAIL / 0 BLOCKED / needFix 0`，Nacos/Server/Worker health 均为健康 |

## 缺陷记录

| 编号 | 级别 | 问题 | 影响 | 状态 |
|---|---|---|---|---|
| BUG-S04-001 | 中 | 协议转换服务已有共享资源类型常量和接收项目访问能力，但系统资源共享后端和前端未接入该类型 | admin 无法在系统管理中共享协议转换服务；接收项目无法通过系统共享获得该协议转换服务 | 已修复并回归通过 |

## 未完成与 BLOCKED

- 本轮无 BLOCKED。
- 更大范围的并发竞态、Token 轮换竞态、日志下载故障注入仍属于后续 S05/S06 深挖范围，本轮只处理资源共享 ACL 中确认的真实缺陷。

## 下一轮建议

- 在共享资源类型全量矩阵上继续做并发编辑/启停/删除测试，重点验证共享记录 upsert、软删恢复、通知重复发送和接收项目可见性。
- 对数据服务、接入服务、协议转换服务的订阅 Token 轮换/禁用做并发 API 探针，确认旧 Token 不可继续访问。
- 对导出、日志下载、详情、运行触发接口继续扩展低权限和跨项目访问矩阵。
