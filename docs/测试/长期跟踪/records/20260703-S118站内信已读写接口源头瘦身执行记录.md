# S118 站内信已读写接口源头瘦身执行记录

## 基本信息

- 测试日期：2026-07-03
- 触发任务：延续 Studio 页面源头瘦身专项，结合 build-nginx 页面和代码复核 `/notifications`。
- Studio 入口：`http://127.0.0.1:8000/dfs/data-aggregation-studio`
- 长期项目：`长期回归测试项目` / `2068077680446365698`
- 长期库：`studio_longterm_regression`
- 本轮数据策略：不清理长期数据；为实服验证将通知 `2072147707808309250` 标记为已读并保留。
- 本轮边界：分页当前页拉取、刷新按钮和 snapshot 摘要刷新属于正常行为；写接口不应为当前动作读取或返回前端未使用的完整通知视图。

## 发现问题

| 编号 | 模块 | 问题 | 是否真实存在 | 是否修复 |
|---|---|---|---|---|
| FIX-S118-001 | 通知 / 站内信已读动作 | `POST /notifications/{id}/read` 只需标记已读，前端不使用返回体，但后端读取 `title/content/targetPath` 等通知视图字段并返回 `NotificationView`；`read-all` 先加载全部未读通知 id 再逐条 `updateById`；无 SSE 订阅者时仍提前构造 snapshot | 是 | 是 |

## 修复范围

| 文件 | 说明 |
|---|---|
| `backend/studio-infra/src/main/java/com/jdragon/studio/infra/service/NotificationService.java` | 单条已读改为只查询 `id/recipientUserId/readAt` marker，使用条件 `LambdaUpdateWrapper` 更新 `readAt`；全部已读改为直接条件更新；`emitSnapshot` 先判断是否存在 SSE 订阅者，再构造 snapshot。 |
| `backend/studio-server/src/main/java/com/jdragon/studio/server/web/controller/NotificationController.java` | `POST /notifications/{id}/read` 改为 `Result<Void>`，不再返回通知视图。 |
| `frontend/packages/api-sdk/src/client.ts` | `notifications.markRead()` 返回类型改为 `void`。 |
| `backend/studio-test/src/test/java/com/jdragon/studio/test/NotificationServiceRegressionTest.java` | 增加单条已读轻量 marker 和全部已读条件更新回归断言。 |

## 测试结果

| 编号 | 模块 | 步骤 | 预期 | 实际 | 结果 | 是否需要继续修复 |
|---|---|---|---|---|---|---|
| S118-TC-001 | 前端写后刷新 | 检查 `NotificationsView.vue` 与 `stores/notifications.ts` | 单条已读不重载整页通知列表；全部已读不重载整页 | 单条已读本地 patch 当前列表项；未读筛选下本地移除；全部已读本地标记或清空未读筛选，仅刷新铃铛 snapshot | PASS | 否 |
| S118-TC-002 | 后端源头修复 | 检查 `NotificationService.markRead/markAllRead/emitSnapshot` | 写接口源头轻量，不读取或返回完整通知视图；批量已读不先拉全量未读行 | 单条只读 `id/recipientUserId/readAt` 并条件更新；全部已读直接条件更新；无 SSE 订阅者不构造 snapshot | PASS | 否 |
| S118-TC-003 | 定向回归 | 执行 `mvn -pl studio-test -am -Dtest=NotificationServiceRegressionTest,NotificationStreamSecurityRegressionTest -Dsurefire.failIfNoSpecifiedTests=false test` | 通知服务与 SSE 安全回归通过 | 6 tests，0 failures，0 errors，BUILD SUCCESS | PASS | 否 |
| S118-TC-004 | 清理后复跑 | 执行 `mvn -pl studio-test -am -Dtest=NotificationServiceRegressionTest -Dsurefire.failIfNoSpecifiedTests=false test` | 新增源头瘦身断言稳定通过 | 4 tests，0 failures，0 errors，BUILD SUCCESS | PASS | 否 |
| S118-TC-005 | 前端构建 | 执行 `npm run build:web` | SDK 类型变更后 web 构建通过 | `vue-tsc --noEmit && vite build` 通过，生成 `NotificationsView-OfwTVQDQ.js` | PASS | 否 |
| S118-TC-006 | server 打包重启 | 执行 `mvn -pl studio-server -am -DskipTests package`，重启 18080 | 新 exec jar 可启动，健康检查通过 | 新 server PID `18492`，`/actuator/health` 返回 `UP` | PASS | 否 |
| S118-TC-007 | build-nginx API 验证 | 登录 admin，调用 `GET /notifications?pageNo=1&pageSize=20` 后对通知 `2072147707808309250` 调用 `POST /notifications/{id}/read` | 响应不再返回通知完整视图 | 登录 `SUCCESS`，列表 `total=164`，已读接口 `SUCCESS` 且 `data=null` | PASS | 否 |
| S118-TC-008 | build-nginx 页面验证 | 浏览器访问并刷新 `/notifications` | 页面可达，长期项目上下文正确，无 console warn/error | 页面显示 `Default Tenant / 长期回归测试项目`、消息列表、分页 `共 164 条`；通知 `2072147707808309250` 已读后不再显示“标记已读”；console warn/error 为 0 | PASS | 否 |

## 结论

- 本轮确认并修复站内信已读写接口的真实源头过取问题。
- 列表和 snapshot 仍保留当前页/最近 10 条摘要读取，属于页面展示和铃铛状态所需。
- 本轮未清理长期数据，已读验证记录保留用于追溯。

## 后续回归入口

- 后端：`NotificationServiceRegressionTest`、`NotificationStreamSecurityRegressionTest`
- 前端：`npm run build:web`
- 页面/API：build-nginx `/notifications`、`POST /notifications/{id}/read`
