# 2026-06-30 S92 质量规则任务列表删除刷新收敛执行记录

## 触发任务

延续 Studio 长期测试目标，继续从 build-nginx 前端可达页面出发，结合源码二次验证“表格接口只取展示字段、详情按 id 下钻、写操作刷新范围不可过大”的执行情况。本轮接续 S91 后的候选点，重点检查质量规则列表和质量任务列表。

## 长期约定

- Studio 入口：`http://127.0.0.1:8000/dfs/data-aggregation-studio`
- 默认账号：`admin / admin123`
- 长期项目：`长期回归测试项目` / `2068077680446365698`
- Nacos：`127.0.0.1:8848`
- 长期数据不清理，本轮未新增、未删除任何长期对象。
- 本轮 UI 复核只读访问质量规则和质量任务列表，不执行删除或批量删除动作，避免破坏可追溯数据。

## 排查范围

| 候选点 | 页面/接口 | 结论 |
|---|---|---|
| 质量规则列表 | `GET /api/v1/quality-rules` | 列表源头字段已在 S39 收敛，由 `QualityListSourceSlimmingRegressionTest` 覆盖；本轮复跑通过 |
| 质量任务列表 | `GET /api/v1/quality-tasks` | 列表源头字段已在 S39 收敛，由 `QualityListSourceSlimmingRegressionTest` 覆盖；本轮复跑通过 |
| 质量规则详情/编辑 | `GET /api/v1/quality-rules/{id}` | 详情页按 id 获取完整规则、SQL、输入输出参数，符合下钻按需读取规则，未调整 |
| 质量任务详情/编辑 | `GET /api/v1/quality-tasks/{id}` | 详情页按 id 获取完整绑定、参数和任务配置，符合下钻按需读取规则，未调整 |
| 质量规则删除/批量删除 | `DELETE /api/v1/quality-rules/{id}`、`POST /api/v1/quality-rules/batch-delete` | 删除成功后前端整页 `loadRules()`，刷新范围超过当前行变化；改为本地移除，只有当前页删空且仍有总数时才补拉 |
| 质量任务删除 | `DELETE /api/v1/quality-tasks/{id}` | 删除成功后前端整页 `loadTasks()`，刷新范围超过当前行变化；改为本地移除，只有当前页删空且仍有总数时才补拉 |

## 问题确认

| 编号 | 模块 | 现象 | 真实存在 | 是否需要修复 |
|---|---|---|---|---|
| S92-QR-001 | 数据质量/质量规则列表写后刷新 | `QualityRulesView.deleteRule()` 和 `batchDelete()` 删除成功后直接调用 `loadRules()`，导致整个列表重新请求 | 是 | 是 |
| S92-QT-001 | 数据质量/质量任务列表写后刷新 | `QualityTasksView.deleteTask()` 删除成功后直接调用 `loadTasks()`，导致整个列表重新请求 | 是 | 是 |

根因：

- S39 已完成质量规则/任务列表源头瘦身，但删除动作仍保留旧的整表重拉逻辑。
- 删除只影响当前表格行、当前页选择状态和总数，除非当前页删空，否则没有必要重新请求当前页列表。

## 修复范围

| 文件 | 变更 |
|---|---|
| `frontend/apps/web/src/views/QualityRulesView.vue` | 单删和批量删除成功后调用 `removeRuleRows(ids)` 本地移除当前页行、更新总数并清理选中 ID；仅当前页删空且总数仍大于 0 时切到上一页并补拉 |
| `frontend/apps/web/src/views/QualityTasksView.vue` | 删除成功后调用 `removeTaskRow(task)` 本地移除当前行并更新 `taskTotal`；仅当前页删空且总数仍大于 0 时切到上一页并补拉 |

## 验证结果

| 用例 | 入口 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| S92-BE-001 | `QualityListSourceSlimmingRegressionTest`、`QualityRuleOptionsSourceSlimmingRegressionTest` | 质量规则/任务列表和规则选项源头瘦身回归不回退 | 6 tests / 0 failures / 0 errors；Maven `BUILD SUCCESS` | PASS |
| S92-FE-001 | `npm run build:web` | 前端类型检查和生产构建成功 | `vue-tsc` 与 Vite build 成功，生成 `QualityRulesView-*.js` 和 `QualityTasksView-*.js` | PASS |
| S92-UI-001 | build-nginx `/quality-rules` | 质量规则列表可达，长期项目上下文正确，无新增前端错误/警告 | 标题为“质量规则”，当前上下文为 `Default Tenant / 长期回归测试项目`，当前筛选结果为空态，console warn/error 为 0 | PASS |
| S92-UI-002 | build-nginx `/quality-tasks` | 质量任务列表可达，长期项目上下文正确，无新增前端错误/警告 | 标题为“质量任务”，当前上下文为 `Default Tenant / 长期回归测试项目`，当前筛选结果为空态，console warn/error 为 0 | PASS |
| S92-UI-003 | 删除刷新收敛 | 不破坏长期数据前提下验证删除逻辑已改为局部移除 | 源码中删除成功后调用 `removeRuleRows(...)` / `removeTaskRow(...)`，仅在当前页空且总数仍大于 0 时调用 `loadRules()` / `loadTasks()`；本轮未执行真实删除 | PASS |

## 执行命令

```powershell
cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\backend
$env:JAVA_HOME='C:\dev\Java\jdk-17.0.12'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
C:\dev\apache-maven-3.8.8\bin\mvn.cmd -pl studio-test -am "-Dtest=QualityListSourceSlimmingRegressionTest,QualityRuleOptionsSourceSlimmingRegressionTest" "-Dsurefire.failIfNoSpecifiedTests=false" test

cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\frontend
npm run build:web
```

## 结论

- 真实问题已修复。
- 质量规则列表和质量任务列表删除后的刷新范围已收敛，不再无条件重拉整个列表。
- 质量规则/任务列表源头字段仍由 S39 既有实现和回归测试保障，本轮复跑未回退。
- 质量规则/任务详情页仍按 id 获取完整配置，符合“下钻详情另取”的规则。
- 本轮没有新增、删除或清理长期数据；现有长期质量数据继续保留，便于后续重测。
