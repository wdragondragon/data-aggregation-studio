# 2026-06-20 M00 长期环境与基础夹具执行记录

## 触发任务

继续执行长期模块细测目标，本轮从主计划 `M00 长期环境与基础夹具` 开始，建立后续模块复测可复用的长期项目、长期 MySQL 库、长期数据源和 Worker 运行前置。

## 固定上下文

| 项目 | 内容 |
|---|---|
| Studio 入口 | `http://127.0.0.1:8000/dfs/data-aggregation-studio/dashboard` |
| 测试账号 | `admin / admin123` |
| 租户 | `Default Tenant` / `default` |
| 长期项目 | `长期回归测试项目` |
| 长期项目编码 | `lt_reg_longterm_project` |
| 长期项目 ID | `2068077680446365698` |
| 长期 MySQL 库 | `studio_longterm_regression` |
| 长期数据策略 | 保留，不清理 |

## 本轮新增或确认的长期对象

| 类型 | 名称/编码 | ID/数量 | 状态 |
|---|---|---:|---|
| Studio 项目 | `长期回归测试项目` / `lt_reg_longterm_project` | `2068077680446365698` | 本轮创建并保留 |
| MySQL 数据源 | `长期回归-客户经营画像数据源` | `2068077811652583425` | 本轮创建并保留 |
| Worker 绑定 | `studio-online-worker-01` 下发到长期项目 | `2068078055157096450` | 本轮创建并保留 |
| MySQL 表 | `lt_reg_customer_profile` | 4 行 | 本轮创建/更新种子数据 |
| MySQL 表 | `lt_reg_customer_order` | 4 行 | 本轮创建/更新种子数据 |
| MySQL 表 | `lt_reg_order_payment` | 3 行 | 本轮创建/更新种子数据 |
| MySQL 表 | `lt_reg_customer_tag_result` | 3 行 | 本轮创建/更新种子数据 |
| MySQL 表 | `lt_reg_quality_exception_sample` | 4 行 | 本轮创建/更新种子数据 |
| MySQL 表 | `lt_reg_api_ingestion_result` | 1 行 | 本轮创建/更新种子数据 |
| MySQL 表 | `lt_reg_protocol_forward_log` | 1 行 | 本轮创建/更新种子数据 |

## 代码边界回顾

本轮执行前检查了以下代码，避免只按 UI 猜测：

| 领域 | 文件/接口 | 结论 |
|---|---|---|
| 系统项目 | `SystemManagementController`、`SystemManagementService.saveProject` | 项目通过 `/api/v1/system/projects` 创建；超级管理员可查看租户下所有项目 |
| 认证上下文 | `StudioAccessService.buildProfile`、`auth.ts` | 超级管理员的项目列表来自租户下全部项目；前端项目切换通过项目下拉刷新 profile |
| 数据源 | `DataSourceController`、`DataSourceService`、`DataSourceSaveRequest` | MySQL 数据源字段为 `host/port/database/userName/password/usePool`，保存依赖当前项目头 |
| 运维中心 | `OpsCenterController`、`OpsCenterService` | Worker 状态来自项目绑定和 Worker 心跳；已下发且在线后运行链路可继续 |

## 用例结果

| 编号 | 用例 | 实际结果 | 结果 | 是否需要修复 |
|---|---|---|---|---|
| M00-01 | 用 UI 确认长期项目存在，不存在则创建并保留 | API 创建长期项目后，浏览器项目下拉可见并成功切换到 `长期回归测试项目`；页面上下文显示 `Default Tenant / 长期回归测试项目` | PASS | 否 |
| M00-02 | 用 MySQL 创建长期库和业务表，重复执行应幂等 | 使用 `CREATE DATABASE IF NOT EXISTS`、`CREATE TABLE IF NOT EXISTS` 和 `ON DUPLICATE KEY UPDATE` 建库建表并写入中文业务种子数据 | PASS | 否 |
| M00-03 | 在数据源中心创建长期 MySQL 数据源，测试连接并保存 | 表单连接测试返回 `AVAILABLE / Connection success`；保存后连接测试返回 `AVAILABLE / Connection success`；UI 数据源列表显示 `可用` | PASS | 否 |
| M00-04 | 数据源发现表，确认长期业务表能被识别 | UI 点击数据源 `更多 -> 发现`，发现弹窗列出 7 张长期表：客户画像、订单、支付、标签结果、质量异常、接入落地、协议转发日志 | PASS | 否 |
| M00-05 | 运维中心确认 Worker 在线与项目授权状态 | 本轮将 `studio-online-worker-01` 下发到长期项目；运维中心显示 `已下发 / ONLINE / 1 / 1`，在线 Worker 指标为 1 | PASS | 否 |

## 关键证据摘要

### MySQL 数据

`studio_longterm_regression` 当前长期表行数：

| 表 | 行数 |
|---|---:|
| `lt_reg_customer_profile` | 4 |
| `lt_reg_customer_order` | 4 |
| `lt_reg_order_payment` | 3 |
| `lt_reg_customer_tag_result` | 3 |
| `lt_reg_quality_exception_sample` | 4 |
| `lt_reg_api_ingestion_result` | 1 |
| `lt_reg_protocol_forward_log` | 1 |

### 数据源连接

| 阶段 | 结果 |
|---|---|
| 当前表单连接测试 | `success=true`，`status=AVAILABLE`，`message=Connection success` |
| 保存后连接测试 | `success=true`，`status=AVAILABLE`，`message=Connection success` |
| 连接历史 UI | `2026/6/20 05:05:50`，`可用`，`手动`，`30 s`，`Connection success` |

### 发现表

UI 发现弹窗已列出：

- `lt_reg_api_ingestion_result`
- `lt_reg_customer_order`
- `lt_reg_customer_profile`
- `lt_reg_customer_tag_result`
- `lt_reg_order_payment`
- `lt_reg_protocol_forward_log`
- `lt_reg_quality_exception_sample`

### Worker

| 项目 | 结果 |
|---|---|
| Worker 组 | `studio-online-worker-01` |
| 下发状态 | `已下发` |
| 组状态 | `ONLINE` |
| 在线/近期 | `1 / 1` |
| Pod | `studio-worker-local` |

## 观察到的问题与风险

| 编号 | 级别 | 现象 | 判定 | 是否需要修复 |
|---|---|---|---|---|
| RISK-M00-001 | 低 | 控制台仍有 Element Plus `type.text` 废弃 API warning | 与上一轮一致，当前功能不受影响 | 暂缓 |
| RISK-M00-002 | 低 | 控制台仍有 Element Plus `el-radio label act as value` 废弃 API warning | 与上一轮一致，当前功能不受影响 | 暂缓 |

本轮未发现页面白屏、主流程保存失败、数据写入错误、权限绕过或长期数据无法复用问题。

## 长期数据保留说明

本轮新增的长期项目、数据源、Worker 绑定、MySQL 库表和种子数据全部保留，后续模块细测直接复用，不做清理。

## 下一步建议

继续执行主计划 `M01 认证、工作台与访问中心`：

- 登录态、错误登录、退出后重定向。
- 404 兜底、语言切换、菜单收起展开。
- 长期项目上下文下的访问中心、站内信、注册登记入口。
- 保留本轮已创建的长期基础夹具作为后续全部模块前置。
