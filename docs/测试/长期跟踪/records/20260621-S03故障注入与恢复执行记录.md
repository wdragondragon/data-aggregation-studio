# 2026-06-21 S03 故障注入与恢复执行记录

## 结论

S03 首轮完成。最终 API 探针结果为 `9 PASS / 0 FAIL / 3 BLOCKED / needFix 0`，浏览器复核数据源、采集运行和运维中心均通过。本轮确认并修复 2 个真实前端可见缺陷：

- `BUG-S03-001`：数据源连接测试失败及历史记录可能暴露 Java 异常类前缀。
- `BUG-S03-002`：运维中心历史运行异常消息可能暴露 Java 异常类前缀。

长期数据按会话规则保留，不清理。

## 固定环境

| 项 | 值 |
|---|---|
| Studio | `http://127.0.0.1:8000/dfs/data-aggregation-studio/dashboard` |
| API | `http://127.0.0.1:18080/api/v1` |
| 账号 | `admin / admin123` |
| 项目 | `长期回归测试项目` / `2068077680446365698` |
| MySQL | `8.140.247.113:13309` / `studio_longterm_regression` |
| Python | `C:\dev\python-envs\studio-longterm-conda311\python.exe` |
| Nacos | IDEA run configuration `NACOS_SERVER=127.0.0.1:8848` |
| Server/Worker | IDEA `StudioServerApplication`、`StudioWorkerApplication` |

## 代码边界

| 域 | 代码边界 | 结论 |
|---|---|---|
| 数据源连接 | `AggregationSourceCapabilityProvider` | 源头连接测试、发现表、HTTP 数据源失败消息需剥离异常类前缀。 |
| 连接健康历史 | `DatasourceConnectionHealthService` | 当前探针结果、健康水合、连接历史接口都需对历史消息做展示级清洗。 |
| 采集调度 | `DataSourceService`、`DatasourceConnectionHealthService`、`CollectionTaskController` | Worker 绑定禁用后触发采集应给出明确无可用 Worker 提示，恢复后可再次成功运行。 |
| 接入失败 | `DataIngestionExecutionSupport`、接入调试/开放调用 | 重复主键失败提示保持业务可读，不暴露堆栈。 |
| 运维中心 | `OpsCenterService` | 运行异常、运行日志异常、数据服务异常、接入异常展示消息需清洗历史 Java 异常前缀。 |

## 自动化入口

```powershell
& 'C:\dev\python-envs\studio-longterm-conda311\python.exe' `
  'DataAggregation\data-aggregation-studio\docs\测试\长期跟踪\scripts\studio_s03_fault_recovery_probe.py'
```

最终执行时间：`2026-06-21T08:23:35`。

## API 与故障注入结果

| 编号 | 模块 | 验证点 | 实际结果 | 结果 | 是否需要修复 |
|---|---|---|---|---|---|
| S03-ENV-001 | 运行态健康 | Nacos、Server、Worker | `OK / UP / UP` | PASS | 否 |
| S03-AUTH-001 | 禁用用户错误恢复 | `lt_reg_s01_disabled_guard` 登录 | `401 UNAUTHORIZED`，`用户已失效` | PASS | 否 |
| S03-WORKER-001 | Worker 下线提示 | 临时禁用长期项目 Worker 绑定后触发增量采集 | `400 BAD_REQUEST`，`No authorized online worker is available for the current project` | PASS | 否 |
| S03-WORKER-002 | Worker 恢复后采集运行 | 恢复 Worker 绑定后触发增量采集 | 新运行 `2068489863365869569`，`SUCCESS`，`COLLECTION_TASK node completed in 1425 ms` | PASS | 否 |
| S03-MYSQL-001 | MySQL 断连安全替代 | 不可达 MySQL 数据源连接测试 | `UNAVAILABLE`，`Connection refused: no further information`，连接历史无 Java/堆栈标记 | PASS | 否 |
| S03-MYSQL-002 | 不可达数据源发现 | 不可达数据源发现表 | HTTP 200，受控空模型列表，消息为 `Cannot create PoolableConnectionFactory (...)`，不含 Java 异常类前缀 | PASS | 否 |
| S03-MYSQL-003 | MySQL 慢查询稳定性 | `select sleep(2)...` | SQL 成功，`executionMs=2756`，订单行数 6 | PASS | 否 |
| S03-ING-001 | 接入失败提示脱敏 | 接入服务重复主键写入 | 返回重复主键业务错误，已写入行 `LT-S03-20260621082327` 未被覆盖 | PASS | 否 |
| S03-OPS-001 | 运维运行域筛选隔离 | 运维中心筛选 `WORKFLOW_NODE + SUCCESS + studio-online-worker-01` | `healthStatus=HEALTHY`，`runTotal=12`，服务/接入事件为 0，运维消息无内部异常标记 | PASS | 否 |
| S03-REDIS-001 | Redis 不可用 | 停 Redis 故障注入 | 共享运行依赖，强停会影响 Server/Worker；需隔离 Redis 环境 | BLOCKED | 否 |
| S03-NACOS-001 | Nacos 短暂不可用 | 停本地 Nacos 故障注入 | 当前 Server/Worker 依赖本地 Nacos；需可控独立 Nacos 容器 | BLOCKED | 否 |
| S03-LOG-001 | 对象存储日志失败 | 对象存储故障注入 | 当前配置由 IDEA 注入对象存储，需独立配置实例验证对象存储失败分支 | BLOCKED | 否 |

## 缺陷与修复

| 编号 | 严重级别 | 现象 | 根因 | 修复 | 回归证据 |
|---|---|---|---|---|---|
| BUG-S03-001 | 中 | 不可达 MySQL 数据源连接测试和连接历史会返回 `java.lang.RuntimeException: ...` | provider 和连接健康历史直接透出异常消息/历史消息 | `AggregationSourceCapabilityProvider` 源头清洗；`DatasourceConnectionHealthService` 对当前结果、健康水合和历史视图清洗 | `AggregationSourceCapabilityProviderRegressionTest`、`DatasourceConnectionHealthServiceTest`；S03 `S03-MYSQL-001` PASS；浏览器数据源页无内部异常标记 |
| BUG-S03-002 | 中 | 运维中心历史运行异常显示 `java.lang.RuntimeException: 不支持的类型对比` | `OpsCenterService` 直接把历史 `RunRecordEntity.message` 等错误字段映射给前端 | 运维中心 run/log/service/ingestion 事件展示消息统一剥离异常类前缀 | `OpsCenterServiceRegressionTest`；S03 `S03-OPS-001` PASS；浏览器运维中心无内部异常标记 |

## 验证命令

```powershell
$env:JAVA_HOME='C:\dev\Java\jdk-17.0.12'
$env:PATH="$env:JAVA_HOME\bin;$env:PATH"
mvn -pl studio-test -am "-Dtest=AggregationSourceCapabilityProviderRegressionTest,DatasourceConnectionHealthServiceTest,OpsCenterServiceRegressionTest" "-Dsurefire.failIfNoSpecifiedTests=false" "-DforkCount=0" test
```

结果：`Tests run: 19, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。

```powershell
& 'C:\dev\python-envs\studio-longterm-conda311\python.exe' `
  'DataAggregation\data-aggregation-studio\docs\测试\长期跟踪\scripts\studio_s03_fault_recovery_probe.py'
```

结果：`PASS 9 / FAIL 0 / BLOCKED 3 / needFix 0`。

## 浏览器验证

| 页面 | URL | 关键证据 | 结果 |
|---|---|---|---|
| 数据源中心 | `/datasources` | `长期回归-S03不可达MySQL故障数据源` 显示 `Connection refused: no further information`，页面文本无 `java.`、`Exception`、`Traceback`、`com.jdragon` | PASS |
| 采集任务日志 | `/collection-task-runs?collectionTaskId=2068153332268711937` | 第一行 `2026-06-21 08:23:09`，状态成功，消息 `COLLECTION_TASK node completed in 1425 ms` | PASS |
| 运维中心 | `/ops-center` | `studio-online-worker-01` 在线；刷新后无内部异常标记；控制台 error/warn 为 0 | PASS |

## 本轮保留数据

| 类型 | 标识 |
|---|---|
| 故障数据源 | `长期回归-S03不可达MySQL故障数据源` / `2068481810725855233` |
| Worker 绑定 | 复用并恢复 `studio-online-worker-01` / `2068078055157096450` |
| 增量采集运行 | 开发与复跑保留 `2068484908647108610`、`2068486787011682306`、最终证据 `2068489863365869569` |
| 接入流水 | `LT-S03-20260621080345`、`LT-S03-20260621081114`、最终证据 `LT-S03-20260621082327` |

## 最终健康状态

| 项 | 状态 |
|---|---|
| Nacos | `OK` |
| Server | `UP` |
| Worker | `UP` |

## 后续建议

- Redis、Nacos、对象存储故障注入需要独立隔离运行实例，避免影响当前长期回归基线。
- 后续涉及错误消息展示的修改，至少复跑 `studio_s03_fault_recovery_probe.py`，并浏览器复核数据源连接历史与运维中心异常列表。
