# 2026-06-20 M07 数据质量执行记录

## 触发任务

继续执行 Studio 长期模块细测，本轮覆盖数据质量规则、质量任务、运行日志、质量指标、问题中心，以及上一轮疑点的代码级二次验证。期间远程 Nacos 不可访问，已先切换为 IDEA run configuration 中的本地 `127.0.0.1:8848` 后继续测试。

## 固定上下文

| 项目 | 内容 |
|---|---|
| Studio 入口 | `http://127.0.0.1:8000/dfs/data-aggregation-studio/dashboard` |
| API | `http://127.0.0.1:18080/api/v1` |
| 测试账号 | `admin / admin123` |
| 租户 | `Default Tenant` / `default` |
| 长期项目 | `长期回归测试项目` / `2068077680446365698` |
| 长期 MySQL 库 | `studio_longterm_regression` |
| 数据源 | `长期回归-客户经营画像数据源` / `2068077811652583425` |
| 模型 | `lt_reg_quality_exception_sample` / `2068089479229038594` |
| 启动方式 | IDEA `StudioServerApplication`、`StudioWorkerApplication`，`NACOS_SERVER=127.0.0.1:8848` |

## 本轮保留数据

### MySQL 样例表

| 表 | 计数 |
|---|---:|
| `lt_reg_quality_exception_sample` 总行数 | 8 |
| M07 样例行数 | 4 |
| M07 已知样例 `QLT-M07-001` ~ `QLT-M07-004` | 4 |
| M07 重复键计数 | 1 |
| M07 手机号缺失计数 | 2 |

### 质量规则

| 名称 | 编码 | ID | 粒度 | 维度 | 状态 |
|---|---|---:|---|---|---|
| 长期回归-客户重复键唯一性规则 | `lt_reg_duplicate_key_uniqueness_rule` | `2068278596113924097` | 表级 | 唯一性 | 启用 |
| 长期回归-客户手机号完整性规则 | `lt_reg_customer_mobile_completeness_rule` | `2068278607002337281` | 字段级 | 完整性 | 启用 |
| 长期回归-质量样例行数规则 | `lt_reg_quality_sample_row_count_rule` | `2068278618553450497` | 表级 | 有效性 | 启用 |

### 质量任务与运行

| 任务 | 编码 | ID | 运行记录 | 运行结果 | 摘要 |
|---|---|---:|---:|---|---|
| 长期回归-客户重复键唯一性质量任务 | `lt_reg_duplicate_key_uniqueness_task` | `2068278641714397186` | `2068278771981176834` | SUCCESS | 触发 1 条告警 |
| 长期回归-客户手机号完整性质量任务 | `lt_reg_customer_mobile_completeness_task` | `2068278658005073922` | `2068278817028001794` | SUCCESS | 触发 1 条告警 |
| 长期回归-质量样例行数质量任务 | `lt_reg_quality_sample_row_count_task` | `2068278671531704322` | `2068278859537272834` | SUCCESS | 返回 1 行，未触发告警 |

> 说明：运行记录状态为 `SUCCESS` 表示质量 SQL 执行成功；告警命中会进入质量问题中心，不等同于运行失败。

### 质量问题

| 问题编号 | 输出字段 | 严重级别 | 状态 | 处置验证 |
|---|---|---|---|---|
| `QI-2068278787361689602` | `duplicate_count` | CRITICAL | INVESTIGATING | 已指派 `Studio Admin`，已完成严重级别、状态和评论流转 |
| `QI-2068278829258592257` | `missing_count` | MEDIUM | OPEN | 保留为手机号完整性告警样例 |

## 代码边界回顾

| 领域 | 文件/接口 | 结论 |
|---|---|---|
| 质量规则 | `QualityRuleController`、`QualityRuleService`、`QualitySqlTemplateService` | 规则解析、语义校验和模板结构校验均由后端执行 |
| 质量任务 | `QualityTaskController`、`QualityTaskService`、`QualityTaskExecutionService` | 任务保存、SQL 预览、语义校验、发布、手动执行链路完整 |
| 指标与问题 | `QualityMetricsController`、`QualityMetricsService`、`QualityIssueService` | 指标总览、资产风险、问题详情和处置动作按项目上下文过滤 |
| 运行日志 | `RunController`、`RunService`、`RunLogProxyService` | 质量任务运行记录通过 `/api/v1/runs?qualityTaskId=...` 查询，日志通过 `/api/v1/runs/{id}/log` 查看 |
| 前端页面 | `QualityMetricsView.vue`、`QualityTaskRunsView.vue` | 页面可达；质量任务日志需要等待接口完成后显示 3 条运行，指标页刷新和 hard reload 均能显示正确数据 |

## 用例结果

| 编号 | 用例 | 实际结果 | 结果 | 是否需要修复 |
|---|---|---|---|---|
| M07-01 | 质量规则列表可达并显示长期规则 | `/quality-rules` 显示 3 条长期规则，含规则编码、维度、粒度、状态和操作入口 | PASS | 否 |
| M07-02 | 质量规则新建页可达 | `/quality-rules/new` 显示基础信息、SQL 模板、语义校验、参数解析、输入/输出参数区 | PASS | 否 |
| M07-03 | 表级唯一性规则保存与发布 | `lt_reg_duplicate_key_uniqueness_rule` 已保存启用，SQL 统计 M07 重复键为 1 | PASS | 否 |
| M07-04 | 字段级完整性规则保存与发布 | `lt_reg_customer_mobile_completeness_rule` 已保存启用，绑定 `mobile` 字段，M07 缺失数为 2 | PASS | 否 |
| M07-05 | 表级行数规则保存与发布 | `lt_reg_quality_sample_row_count_rule` 已保存启用，M07 行数为 4，未触发告警 | PASS | 否 |
| M07-06 | 质量任务列表可达并显示长期任务 | `/quality-tasks` 显示 3 条长期质量任务，状态均为已发布 | PASS | 否 |
| M07-07 | 质量任务新建页可达 | `/quality-tasks/new` 显示任务绑定、规则选择、SQL 预览、语义校验、发布、手动执行入口 | PASS | 否 |
| M07-08 | 质量任务执行与日志 | 3 条任务均生成运行记录；每条 `/runs/{id}/log` 均返回 Worker 执行日志内容 | PASS | 否 |
| M07-09 | 质量任务运行页 | `/quality-task-runs` 稳定加载后显示运行次数 3、成功 3、失败 0、运行中 0，并列出三条长期任务运行 | PASS | 否 |
| M07-10 | 质量指标总览 | 指标接口和 UI 稳定态显示执行健康分 76、治理风险指数 50、活跃问题数 2、受影响资产 1、纳管资产 1 | PASS | 否 |
| M07-11 | 质量问题中心 | 问题接口返回 2 条活跃问题；重复键问题完成指派、严重级别变更、状态变更和评论记录 | PASS | 否 |
| M07-12 | 表级规则误用 `${Column}` 二次验证 | `POST /quality-rules/validate` 传 `granularity=TABLE` 和包含 `${Column}` 的 SQL，返回 `valid=false`，提示表级规则不允许引用 `${Column}` | PASS | 否 |
| M07-13 | 控制台异常检查 | 未观察到 Studio error；仍有既有 Element Plus pagination 废弃 warning | PASS | 否，暂缓 |

## 关键接口证据

| 接口 | 结果摘要 |
|---|---|
| `GET /api/v1/quality-tasks?pageNo=1&pageSize=20` | 返回 `total=3`，三条长期质量任务均为 `ONLINE` |
| `GET /api/v1/runs?qualityTaskId=2068278641714397186&includeRunRecords=true` | 返回 1 条运行记录 `2068278771981176834`，状态 `SUCCESS` |
| `GET /api/v1/runs?qualityTaskId=2068278658005073922&includeRunRecords=true` | 返回 1 条运行记录 `2068278817028001794`，状态 `SUCCESS` |
| `GET /api/v1/runs?qualityTaskId=2068278671531704322&includeRunRecords=true` | 返回 1 条运行记录 `2068278859537272834`，状态 `SUCCESS` |
| `POST /api/v1/quality-metrics/dashboard/query` | `executionHealthScore=76`、`governanceRiskScore=50`、`activeIssueCount=2` |
| `POST /api/v1/quality-metrics/assets/query` | 返回 1 个高风险资产：`lt_reg_quality_exception_sample` |
| `POST /api/v1/quality-metrics/issues/query` | 返回 2 条活跃质量问题 |

## 观察与判定

| 编号 | 现象 | 复核结论 | 是否需要修复 |
|---|---|---|---|
| OBS-M07-001 | 曾观察到质量任务日志页短时间显示 0 条 | 延长等待后显示 3 条；接口返回正常，判定为测试读取过早 | 否 |
| OBS-M07-002 | 曾观察到质量指标页首次读取为 0 | 点击刷新与 hard reload 均能稳定显示 76/50/2；接口经 7 天窗口查询正常，未稳定复现 | 否，后续迭代继续观察 |
| OBS-M07-003 | 表级规则引用 `${Column}` 疑似被放行 | 代码和接口均验证已正确拒绝，未复现为真实缺陷 | 否 |
| RISK-M07-001 | 控制台仍有 Element Plus `ElPagination` 废弃用法 warning | 与 M00-M06 一致，不影响主流程 | 暂缓 |

## 结论

M07 数据质量模块本轮通过。未发现需要立即修复的真实问题；未修改业务代码。长期保留质量规则、任务、运行记录、问题和 MySQL 样例数据，用于后续工作流、运维和快速迭代回归。
