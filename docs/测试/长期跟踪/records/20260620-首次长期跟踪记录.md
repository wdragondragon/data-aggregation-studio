# 2026-06-20 首次长期跟踪记录

## 记录背景

- 用户要求在当前会话中对 Studio 进行长期测试跟踪。
- 跟踪范围包括：前期全覆盖测试、后续模块细测、快速迭代测试。
- 新策略要求：长期使用同一个测试库和测试项目，测试数据不可清理，以便可追溯、可重测。

## 长期策略生效

从本记录开始，后续 Studio 测试默认遵循：

- 使用长期 MySQL 测试库：`studio_longterm_regression`。
- 使用长期 Studio 测试项目：`长期回归测试项目`。
- 长期测试数据保留，不在测试结束后清理。
- 每次测试前先阅读 `docs/测试/长期跟踪/README.md` 和近期 `records`。
- 每次测试后在本目录追加记录。

## 上一次全覆盖测试基线

| 项目 | 内容 |
|---|---|
| 测试日期 | 2026-06-19 |
| 报告文件 | `docs/测试/全量前端/studio-frontend-full-coverage-test-cases-20260619.md` |
| 结果文件 | `docs/测试/全量前端/studio-frontend-full-coverage-test-result-20260619.md` |
| 测试入口 | `http://127.0.0.1:8000/dfs/data-aggregation-studio/dashboard` |
| 测试账号 | `admin / admin123` |
| 当时上下文 | `Default Tenant / Default Project` |
| 当时数据策略 | 一次性 `e2e_20260619_` / `test_20260619_` 数据，测试结束后已清理 |

## 全覆盖测试结论摘要

| 模块 | 结果摘要 |
|---|---|
| 认证与基础框架 | 登录、上下文、语言切换、未授权访问、404 兜底大部分通过；发现退出登录问题 |
| 路由覆盖 | dashboard、资产、采集、开发、服务、质量、运维、通知、系统页面可达 |
| 数据资产 | MySQL 数据源、发现表、模型同步、模型预览、统计/目录/血缘页面可用 |
| 数据开发 | 目录、SQL/Java/Python 脚本、SQL 执行和错误 SQL 提示可用 |
| 数据服务 | 模型服务、SQL 服务、发布、订阅、调试、开放 API、监控日志可用 |
| 数据接入 | REST 接入服务保存、发布、订阅、调试写入、监控日志可用 |
| 协议转换 | 保存、发布、订阅、调试、日志、下线删除可用 |
| 数据采集 | 全量、SQL/增量、融合三类任务可预览、保存、上线；触发被 Worker 前置阻断 |
| 数据质量 | 规则、任务、预览、校验、指标页可用；问题处理依赖 Worker 运行结果 |
| 工作流与运维 | 工作流保存和发布可用；触发被 Worker 前置阻断；运维中心明确提示 Worker 风险 |
| 系统管理 | 用户、租户、项目、成员、注册审核、Worker 下发、资源共享覆盖；发现成员审批缺陷 |

## 已发现并处理的问题

| 编号 | 模块 | 问题 | 二次验证 | 修复状态 | 回归结果 |
|---|---|---|---|---|---|
| BUG-001 | 认证 | 点击“退出登录”未退出，仍停留 dashboard | 真实存在 | 已修复 | 退出后进入 `/login`；再访问 `/dashboard` 重定向到 `/login?redirect=/dashboard` |
| BUG-002 | 系统管理 | 删除项目成员后审批同一用户邀请返回 500，唯一键冲突 | 真实存在 | 已修复 | 新增回归测试通过，审批路径复活已删除成员，不重复插入 |

## 当前阻断项

| 编号 | 模块 | 阻断原因 | 后续跟踪建议 |
|---|---|---|---|
| BLOCK-001 | Worker/调度 | 当前项目没有授权在线 Worker，任务触发返回 `No authorized online worker is available for the current project` | 长期测试项目建立后，需要绑定可用 Worker，再复测采集、质量、工作流运行链路 |
| BLOCK-002 | 质量问题处理 | 质量任务未能进入运行阶段，未生成质量问题 | 依赖 Worker 问题解决后，补测问题详情、指派、状态、严重度、评论 |

## 长期数据说明

- 2026-06-19 全覆盖测试使用的是一次性数据，已按当时计划清理，不作为长期追踪数据。
- 后续不得继续默认清理长期测试数据。
- 若需要临时隔离测试，必须在记录中明确标注“一次性隔离数据”，并与长期测试数据区分。

## 下一次测试前检查清单

- 确认或创建 Studio 项目：`长期回归测试项目`。
- 确认或创建 MySQL 库：`studio_longterm_regression`。
- 复用或增量创建长期测试对象，展示名使用中文业务名。
- 确认 Worker 是否可用；若仍不可用，继续将运行链路标记为 `BLOCKED`。
- 测试结束后追加记录，不清理长期数据。
