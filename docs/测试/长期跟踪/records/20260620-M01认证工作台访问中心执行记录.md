# 2026-06-20 M01 认证、工作台与访问中心执行记录

## 触发任务

用户要求继续推进 Studio 长期模块细测。本轮基于长期跟踪约定执行 `M01 认证、工作台与访问中心`，覆盖登录、登出、受保护路由、404 兜底、语言和菜单、站内信入口、注册登记、无项目访问中心、项目申请与取消。

## 执行前回顾

本轮开始前已回顾：

| 类型 | 文件 |
|---|---|
| 长期约定 | `DataAggregation/data-aggregation-studio/docs/测试/长期跟踪/README.md` |
| 主计划 | `DataAggregation/data-aggregation-studio/docs/测试/长期跟踪/studio-module-detailed-test-master-plan-20260620.md` |
| 最近记录 | `records/20260620-首次长期跟踪记录.md` |
| 最近记录 | `records/20260620-模块细测主计划与路由验证.md` |
| 最近记录 | `records/20260620-M00长期环境与基础夹具执行记录.md` |

长期约定继续生效：

- 长期项目：`长期回归测试项目` / `lt_reg_longterm_project` / `2068077680446365698`。
- 长期库：`studio_longterm_regression`。
- 长期数据保留，不清理。
- 新增展示名使用中文业务名称，技术编码使用 `lt_reg_`。

## 代码边界回顾

本轮执行前结合代码确认了 M01 入口和主要行为：

| 领域 | 文件 | 结论 |
|---|---|---|
| 路由守卫 | `frontend/apps/web/src/router/index.ts` | 未登录受保护路由重定向 `/login?redirect=...`；无项目用户重定向 `/access-center`；未知路由重定向 `/dashboard` |
| 登录/登出 | `frontend/apps/web/src/views/LoginView.vue`、`frontend/apps/web/src/stores/auth.ts`、`frontend/apps/web/src/layout/StudioLayout.vue` | 本地账号登录后写入 token；退出调用 `authStore.logout()` 并 `router.replace("/login")` |
| 注册登记 | `frontend/apps/web/src/views/RegisterView.vue`、`UserRegistrationRequestService` | 公开提交注册登记；同用户名存在 `PENDING` 时拒绝重复提交；审批通过后创建用户但不直接加入项目 |
| 访问中心 | `frontend/apps/web/src/views/WorkspaceAccessView.vue`、`WorkspaceAccessService` | 有项目上下文时自动回 dashboard；无项目用户可查看可加入项目、提交申请、取消本人 pending 申请 |
| 站内信 | `NotificationBell.vue`、`NotificationsView.vue`、`notifications.ts`、`NotificationService` | 支持未读快照、消息中心、单条已读、全部已读和 SSE 刷新 |

## 本轮新增且保留的长期数据

| 类型 | 名称/账号 | ID | 状态 | 说明 |
|---|---|---:|---|---|
| 注册登记 | `lt_reg_m01_kehu_yunying_guanchayuan_20260620_053203` / `长期回归-客户运营观察员` | `2068084425335988226` | `PENDING` | PowerShell API 批次，保留用于重复注册登记观察 |
| 注册登记 | `lt_reg_m01_xiangmu_shenqing_zhuanyuan_20260620_053203` / `长期回归-项目申请专员` | `2068084430297849857` | `APPROVED` | PowerShell API 批次，审批后用户 ID `2068084435591061505` |
| 注册登记 | `lt_reg_m01_kehu_yunying_guanchayuan_20260619213300` / `长期回归-客户运营观察员` | `2068084660053434369` | `PENDING` | UTF-8 API 批次，重复提交返回 `400` |
| 注册登记 | `lt_reg_m01_xiangmu_shenqing_zhuanyuan_20260619213300` / `长期回归-项目申请专员` | `2068084664058994690` | `APPROVED` | UTF-8 API 批次，审批后用户 ID `2068084669159268354` |
| 注册登记 | `ltregm01xiangmushenqingzhuanyuan20260619213437` / `长期回归-访问中心申请专员` | `2068085068633169922` | `APPROVED` | UI 登录专用账号，审批后用户 ID `2068085074366783489` |
| 项目加入申请 | 用户 `ltregm01xiangmushenqingzhuanyuan20260619213437` 申请 `长期回归测试项目` | `2068085263857049601` | `CANCELLED` | 通过访问中心 UI 提交并取消，保留流水 |

说明：浏览器输入通道本轮不能稳定输入中文，中文展示名和中文申请说明通过 Studio API 辅助写入，并在系统管理 UI 中验证展示正确。PowerShell 输出曾出现终端编码乱码，但系统管理 UI 与后续接口查询显示数据实际为中文。

## 用例结果

| 编号 | 用例 | 前置数据 | 实际结果 | 结果 | 是否需要修复 |
|---|---|---|---|---|---|
| M01-01 | 正确账号登录进入工作台，展示当前租户/项目/用户 | `admin / admin123` | UI 登录成功进入 `/dashboard`；项目下拉切换到 `长期回归测试项目`；页面显示 `Default Tenant / 长期回归测试项目 / admin` | PASS | 否 |
| M01-02 | 错误密码、空用户名、空密码校验 | 登录页 | 空表单停留登录页并提示 `username: Username is required; password: Password is required`；错误密码提示 `用户名或密码错误` | PASS | 暂缓，空表单文案为英文 |
| M01-03 | 退出登录后访问受保护路由重定向登录 | 已登录 admin | 点击 `退出登录` 进入 `/login`；再访问 `/dashboard` 被重定向到 `/login?redirect=/dashboard` | PASS | 否 |
| M01-04 | 直接访问不存在路由回 dashboard | 已登录 admin | 访问 `/not-a-real-m01-route` 后短暂空白，约 2 秒内跳转 `/dashboard` 并渲染总览 | PASS | 否 |
| M01-05 | 中英文切换、菜单收起展开、站内信入口 | 已登录 admin / 长期项目 | 切换 English 后标题为 `Dashboard | Data Aggregation Studio`；切回中文后为 `总览 | 数据聚合工作台`；收起侧边栏显示 `>>` 和短菜单，展开恢复完整菜单；站内信铃铛可打开消息弹层并进入消息中心 | PASS | 否 |
| M01-06 | 无项目上下文访问中心，项目申请/取消申请 | `长期回归-访问中心申请专员` | 该账号登录后自动进入 `/access-center`；页面显示暂未绑定项目和可申请项目；对 `长期回归测试项目` 提交申请后状态 `PENDING`，项目卡片显示 `审批中`；取消后状态 `CANCELLED`，项目卡片恢复 `申请加入`；`刷新权限` 保持访问中心并提示权限未更新 | PASS | 暂缓，取消备注为英文 `Cancelled by applicant` |
| M01-07 | 注册登记提交、审核入口展示和重复账号提示 | 长期注册登记数据 | 注册页 `/register` 可达；提交登记 API 成功；同用户名重复提交返回 `400` 和 `A pending registration request already exists for this username`；系统管理 `/system?tab=registrationRequests` 展示 PENDING/APPROVED 记录、中文显示名、中文申请说明和审核信息 | PASS | 暂缓，重复提示为英文 |
| M01-08 | 通知中心列表、单条已读、全部已读 | admin 有历史未读登记通知 | 从铃铛进入消息中心；单条已读后状态刷新；全部已读后铃铛未读数清零，列表不再显示 `标记已读`；控制台无 error | PASS | 否 |

## 关键证据摘要

| 证据 | 内容 |
|---|---|
| 登录成功 | `/dashboard`，标题 `总览 | 数据聚合工作台`，上下文 `Default Tenant / 长期回归测试项目` |
| 登出重定向 | `/login`，再次访问 `/dashboard` 得到 `/login?redirect=/dashboard` |
| 404 兜底 | `/not-a-real-m01-route` 最终回 `/dashboard` |
| 访问中心账号 | `ltregm01xiangmushenqingzhuanyuan20260619213437` 登录后自动进入 `/access-center` |
| 项目申请流水 | `2068085263857049601`，`APPLY / CANCELLED`，项目 `长期回归测试项目` |
| 注册重复 | 同用户名重复登记返回 `400 BAD_REQUEST` |
| 注册审核 UI | 系统管理注册登记 tab 显示 `长期回归-客户运营观察员`、`长期回归-项目申请专员`、`长期回归-访问中心申请专员` |
| 通知中心 | `全部已读` 后铃铛不再显示未读数字，列表无 `标记已读` |
| 控制台 | 0 条 error；仍有 Element Plus 废弃 API warning |

## 观察到的问题与风险

| 编号 | 级别 | 现象 | 当前判定 | 是否需要修复 |
|---|---|---|---|---|
| OBS-M01-001 | 低 | 中文 UI 下空登录校验提示为英文：`username: Username is required; password: Password is required` | 功能正确，文案可读但未本地化 | 可暂缓 |
| OBS-M01-002 | 低 | 中文 UI 下重复注册登记提示为英文：`A pending registration request already exists for this username` | 功能正确，文案可读但未本地化 | 可暂缓 |
| OBS-M01-003 | 低 | 访问中心取消申请后的审批备注为英文：`Cancelled by applicant` | 功能正确，中文 UI 文案不一致 | 可暂缓 |
| RISK-M01-001 | 低 | 直接访问未知路由时先出现短暂空白，随后回 dashboard | 最终状态正确，未形成白屏 | 否 |
| RISK-M01-002 | 低 | 控制台仍有 Element Plus `type.text` 废弃 API warning | 与 M00 一致，功能不受影响 | 可暂缓 |
| RISK-M01-003 | 低 | 控制台仍有 Element Plus `el-radio label act as value` 废弃 API warning | 与 M00 一致，功能不受影响 | 可暂缓 |

本轮未发现页面最终白屏、路由不可达、主链路保存/发布/执行失败、权限绕过、控制台 error 或长期数据无法复用问题。

## 修复结论

本轮未执行源码修复。

原因：

- 所有 M01 主链路用例最终结果为 `PASS`。
- 发现项集中在中文 UI 下的英文文案和短暂加载过渡，按当前规则判定为可暂缓，不影响认证、访问申请、注册登记和通知主流程。
- 后续若用户要求提升本地化一致性，可集中修复 `LoginRequest` 校验消息、`UserRegistrationRequestService` 重复提示和 `WorkspaceAccessService.cancel` 的取消备注。

## 长期数据保留说明

本轮创建的注册登记、审批用户、访问中心申请流水全部保留，不清理。后续模块或快速迭代测试可复用这些用户和流水：

- `长期回归-客户运营观察员`：保留 PENDING 登记，用于重复注册和审核入口复测。
- `长期回归-项目申请专员`：保留已审批无项目用户，用于访问中心和审批通知复测。
- `长期回归-访问中心申请专员`：保留已审批无项目用户和已取消申请流水，用于访问中心状态复测。

## 下一步建议

继续执行主计划 `M02 数据资产、目录与模型`：

- 复用长期项目 `长期回归测试项目`。
- 复用长期 MySQL 数据源 `长期回归-客户经营画像数据源`。
- 验证目录、元数据、数据源、发现表、模型同步、模型预览、统计筛选、血缘/目录页面。
- 保留新增模型、目录和同步任务数据，不清理。
