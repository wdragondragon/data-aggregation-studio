# S117 运维中心源头复核执行记录

## 基本信息

- 测试日期：2026-07-03
- 触发任务：延续 Studio 页面源头瘦身专项，结合 build-nginx 页面和代码复核 `/ops-center`。
- Studio 入口：`http://127.0.0.1:8000/dfs/data-aggregation-studio`
- 长期项目：`长期回归测试项目` / `2068077680446365698`
- 长期库：`studio_longterm_regression`
- 本轮数据策略：未新增、未清理长期数据。
- 本轮边界：分页当前页拉取、刷新按钮、自动刷新和运行状态刷新属于正常行为；只判定列表或轻量入口是否源头读取完整日志、大 JSON、详情对象或跨页面无关数据。

## 复核范围

| 范围 | 结论 |
|---|---|
| 运维中心前端加载 | `OpsCenterView.vue` 首屏并行加载 overview、队列、Worker、运行异常、服务异常、接入异常和日志异常；各表格固定 `pageSize=8`，属于仪表盘当前页摘要加载。 |
| 队列与运行异常 | `queryQueue()`、`queryRuns()`、`queryLogEvents()` 均走 `selectPage`；`DispatchTaskEntity`、`RunRecordEntity` 查询均字段级 `select`，未读取 `payloadJson/resultJson/logFilePath/logObjectKey` 等详情或日志对象字段。 |
| 服务与接入异常 | `queryServiceEvents()`、`queryIngestionEvents()` 均走 `selectPage`；访问日志查询字段级 `select`，未读取 `systemLog/logObjectKey/userAgent` 等完整日志或客户端详情字段。 |
| Worker 状态 | `workerGroups()` 只读取项目绑定和 Worker lease 展示所需字段；未读取 Worker capabilities/config 等大字段。 |
| 队列对象名称解析 | `TargetNameResolver` 只按 id 查询工作流、采集任务、质量任务的名称轻字段；未读取完整定义 JSON。 |
| Overview 统计 | `overview()` 按当前时间窗口读取轻字段后做健康统计；本轮未发现把不可见大字段带入前端。结合当前边界，该统计行为不按缺陷处理。 |
| build-nginx 页面 | `/ops-center` 显示 `Default Tenant / 长期回归测试项目`，健康卡片、队列、Worker、运行异常、服务异常、接入异常和日志异常区块均正常渲染，console warn/error 为 0。 |

## 测试结果

| 编号 | 模块 | 步骤 | 预期 | 实际 | 结果 | 是否需要修复 |
|---|---|---|---|---|---|---|
| S117-TC-001 | 前端加载行为 | 检查 `OpsCenterView.vue` 的 `loadAll()`、自动刷新和筛选刷新逻辑 | 首屏和刷新只请求当前页面摘要，不把分页或刷新行为误判为缺陷 | 各列表固定 `pageNo=1/pageSize=8`，自动刷新为 30 秒重载当前摘要 | PASS | 否 |
| S117-TC-002 | 后端分页与字段复核 | 检查 `OpsCenterService` 中队列、运行、服务、接入、日志异常查询 | 表格区使用 DB 分页，字段级读取可见字段 | `queryQueue/queryRuns/queryServiceEvents/queryIngestionEvents/queryLogEvents` 均为 `selectPage`，查询 wrapper 显式 `select` 轻字段 | PASS | 否 |
| S117-TC-003 | 详情大字段排查 | 对照实体中的 `payloadJson/resultJson/systemLog/logObjectKey/logFilePath/capabilitiesJson` | 轻量入口不读取完整日志、大 JSON、详情对象或 Worker 配置 | 运维中心列表和状态查询未选中这些字段；名称解析只读 `id/name/taskName` | PASS | 否 |
| S117-TC-004 | 后端定向回归 | 执行 `mvn -pl studio-test -am -Dtest=OpsCenterServiceRegressionTest -Dsurefire.failIfNoSpecifiedTests=false test` | 运维中心健康、筛选、分页、消息清洗和作用域回归通过 | 14 tests，0 failures，0 errors，BUILD SUCCESS | PASS | 否 |
| S117-TC-005 | build-nginx 页面复核 | 浏览器访问 `/ops-center` | 页面可达，长期项目上下文正确，无 console warn/error | 页面显示长期项目、健康状态、Worker 组和各异常区块；warn/error 为 0 | PASS | 否 |

## 结论

- 本轮未发现 `/ops-center` 存在真实需要修复的源头过取问题。
- 不修改业务代码。
- 当前页分页拉取、刷新按钮、自动刷新和状态刷新继续视为正常行为；后续若出现大时间窗口慢查询，再单独评估 overview 是否需要进一步改为数据库聚合。

## 后续回归入口

- 后端：`OpsCenterServiceRegressionTest`
- 页面：build-nginx `/ops-center`
- 代码关注点：`OpsCenterView.vue`、`OpsCenterService`、`OpsCenterController`
