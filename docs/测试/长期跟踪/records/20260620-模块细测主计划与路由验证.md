# 2026-06-20 模块细测主计划与路由验证

## 触发任务

用户在长期测试跟踪建立后要求继续推进。本轮目标是基于上一次全覆盖测试和当前代码，拆分 Studio 模块边界，形成后续模块细测的长期主计划，并用浏览器确认当前运行态主路由不是纯代码推断。

## 回顾的长期约定

- 长期测试数据库：`studio_longterm_regression`。
- 长期测试项目：`长期回归测试项目`。
- 后续测试数据保留，不清理。
- 新增测试对象使用可读的中文业务名称，并辅以 `lt_reg_` 技术编码。
- 每次测试前回顾 `docs/测试/长期跟踪/README.md` 和近期 `records/`。

## 本轮覆盖范围

| 类型 | 内容 |
|---|---|
| 文档回顾 | `README.md`、`20260620-首次长期跟踪记录.md` |
| 前端代码 | `frontend/apps/web/src/router/index.ts`、`frontend/apps/web/src/views` |
| 后端代码 | `backend/studio-server/src/main/java/com/jdragon/studio/server/web/controller` |
| 运行页面 | 主菜单路由和主要 `/new` 编辑页 |
| 新增文档 | `studio-module-detailed-test-master-plan-20260620.md` |

## 浏览器验证摘要

账号：`admin / admin123`

上下文：`Default Tenant / Default Project`

说明：本轮只做菜单/路由抽样验证，没有创建、修改或删除长期业务数据。后续正式模块细测应切换或创建到 `长期回归测试项目`。

已验证可渲染主内容且未重定向登录的页面：

| 类型 | 路由 |
|---|---|
| 工作台与资产 | `/dashboard`、`/catalog`、`/metadata`、`/datasources`、`/models`、`/statistics` |
| 采集 | `/field-mapping-rules`、`/collection-tasks`、`/collection-task-runs`、`/run-metrics` |
| 开发 | `/data-development`、`/workflows`、`/runs` |
| 服务 | `/data-services`、`/data-ingestion-services`、`/protocol-conversions`、`/data-ingestion-metrics`、`/data-service-metrics` |
| 质量 | `/quality-rules`、`/quality-tasks`、`/quality-task-runs`、`/quality-metrics` |
| 运维与系统 | `/ops-center`、`/notifications`、`/system` |
| 新建页 | `/data-services/new`、`/data-ingestion-services/new`、`/protocol-conversions/new`、`/field-mapping-rules/new`、`/collection-tasks/new`、`/quality-rules/new`、`/quality-tasks/new`、`/workflows/new` |

## 观察到的运行态风险

| 编号 | 级别 | 现象 | 当前判定 | 后续处理 |
|---|---|---|---|---|
| RISK-20260620-001 | 低 | Element Plus 控制台 warning：`type.text` 将在 3.0.0 废弃 | 可暂缓 | 升级 Element Plus 前纳入技术债清理 |
| RISK-20260620-002 | 低 | Element Plus 控制台 warning：`el-radio label act as value` 将在 3.0.0 废弃 | 可暂缓 | 升级 Element Plus 前纳入技术债清理 |

未观察到主路由白屏、登录失效或路由重定向异常。

## 新增产物

- `DataAggregation/data-aggregation-studio/docs/测试/长期跟踪/studio-module-detailed-test-master-plan-20260620.md`

该主计划按 M00 至 M10 拆分：

| 模块 | 主题 |
|---|---|
| M00 | 长期环境与基础夹具 |
| M01 | 认证、工作台与访问中心 |
| M02 | 数据资产、目录与模型 |
| M03 | 数据开发 |
| M04 | 数据服务 |
| M05 | 数据接入与协议转换 |
| M06 | 数据采集与指标 |
| M07 | 数据质量 |
| M08 | 工作流、调度与运维 |
| M09 | 系统管理与权限 |
| M10 | 通知、关注、导入导出与日志下载 |

## 长期数据变化

本轮没有创建、修改或清理长期测试数据。

## 当前结果

| 项目 | 结果 |
|---|---|
| 长期规则回顾 | PASS |
| 代码边界梳理 | PASS |
| 运行路由抽样 | PASS |
| 主计划落文档 | PASS |
| 长期数据保留约束 | PASS |

## 下一步建议

下一轮正式执行建议从 M00 开始：

- 确认或创建 `studio_longterm_regression`。
- 确认或创建 `长期回归测试项目`。
- 创建长期 MySQL 业务表和长期数据源。
- 检查 Worker 是否可绑定到长期项目。
- 若 Worker 仍不可用，先继续资产、开发、服务、接入、规则配置类测试，把采集/质量/工作流运行链路标记为 `BLOCKED`。
