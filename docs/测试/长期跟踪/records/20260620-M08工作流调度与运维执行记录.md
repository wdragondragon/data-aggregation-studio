# 2026-06-20 M08 工作流调度与运维执行记录

## 触发任务

继续执行 Studio 长期模块细测，本轮覆盖工作流编排、工作流运行、节点日志、统一运维中心和 Worker 状态。执行中结合后端代码发现并修复 1 个运维中心筛选语义缺陷。

## 固定上下文

| 项目 | 内容 |
|---|---|
| Studio 入口 | `http://127.0.0.1:8000/dfs/data-aggregation-studio/dashboard` |
| API | `http://127.0.0.1:18080/api/v1` |
| 测试账号 | `admin / admin123` |
| 租户 | `Default Tenant` / `default` |
| 长期项目 | `长期回归测试项目` / `2068077680446365698` |
| 长期 MySQL 库 | `studio_longterm_regression` |
| 启动方式 | IDEA `StudioServerApplication`、`StudioWorkerApplication`，`NACOS_SERVER=127.0.0.1:8848` |
| Server health | `UP` |
| Worker health | `UP` |

## 本轮保留数据

### 工作流

| 项 | 内容 |
|---|---|
| 工作流名称 | `长期回归-客户经营数据日处理流程` |
| 工作流编码 | `lt_reg_customer_daily_workflow` |
| 工作流 ID | `2068289624298184705` |
| 当前版本 ID | `2068289624960884737` |
| 发布状态 | 已发布 |
| 调度状态 | 关闭 |
| Cron | `0 */30 * * * ?` |
| 时区 | `Asia/Shanghai` |

### 工作流节点

| 节点编码 | 节点名称 | 类型 | 绑定对象 |
|---|---|---|---|
| `collect_order_payment_fusion` | `融合采集-订单支付事实宽表` | `COLLECTION_TASK` | `长期回归-订单支付融合采集任务` / `2068153361725308929` |
| `quality_sample_row_count` | `质量校验-样例表行数` | `QUALITY_TASK` | `长期回归-质量样例行数质量任务` / `2068278671531704322` |
| `script_customer_value_query` | `脚本查询-客户价值分层` | `DATA_SCRIPT` | `长期回归-客户价值查询.sql` / `2068102548877467649` |

### 本轮新增运行记录

| 项 | 内容 |
|---|---|
| 工作流运行 ID | `2068295084980002817` |
| 开始时间 | `2026-06-20 19:29:13` |
| 结束时间 | `2026-06-20 19:29:29` |
| 状态 | `SUCCESS` |
| 节点统计 | 总节点 3、成功 3、失败 0、运行中 0、未运行 0 |
| 摘要 | `3/3 node(s) completed` |

| 节点 | 运行记录 ID | 状态 | 摘要 |
|---|---:|---|---|
| `collect_order_payment_fusion` | `2068295097726464001` | SUCCESS | `COLLECTION_TASK node completed in 681 ms (fusion -> mysql8)` |
| `quality_sample_row_count` | `2068295135194181633` | SUCCESS | `长期回归-质量样例行数质量任务 executed successfully, returned 1 row(s)` |
| `script_customer_value_query` | `2068295159927992321` | SUCCESS | `长期回归-客户价值查询.sql completed in 654 ms with 4 row(s) returned` |

> 说明：上一轮已存在工作流运行 `2068290168391688194`，本轮未清理，运行列表按工作流筛选共显示 2 次长期工作流运行。

## 代码边界回顾

| 领域 | 文件/接口 | 结论 |
|---|---|---|
| 工作流定义 | `WorkflowController`、`WorkflowService` | 列表、详情、保存草稿、发布、删除；保存会校验至少 1 个节点、同项目 code/name 唯一 |
| 工作流触发 | `WorkflowController.trigger`、`DispatchService` | 手动触发后生成工作流级运行和 3 个节点运行记录 |
| 工作流运行 | `WorkflowRunController`、`WorkflowRunService` | 支持按工作流、状态、时间分页筛选；详情合并节点定义和运行记录 |
| 运行日志 | `RunController`、`RunLogProxyService`、`RunLogDrawer.vue` | 节点日志通过 `/runs/{runRecordId}/log` 和 `/download` 查看；UI 抽屉能显示完整日志 |
| 统一运维 | `OpsCenterController`、`OpsCenterService`、`OpsCenterView.vue` | 汇总队列、Worker、运行异常、服务异常、接入异常和日志异常；运行域筛选已修复隔离服务/接入异常 |
| 前端工作流 | `WorkflowEditorView.vue`、`WorkflowCanvasSection.vue`、`WorkflowRunDetailView.vue` | 编辑页、详情页、运行列表和日志抽屉均可达 |

## 用例结果

| 编号 | 用例 | 实际结果 | 结果 | 是否需要修复 |
|---|---|---|---|---|
| M08-01 | 工作流列表可达并显示长期工作流 | `/workflows` 显示长期工作流，含编码、项目、已发布、调度关闭 | PASS | 否 |
| M08-02 | 工作流新建页可达 | `/workflows/new` 显示基础信息、Cron、调度开关和 5 类节点调色板：采集任务、质量任务、数据脚本、HTTP、Shell | PASS | 否 |
| M08-03 | 工作流详情页可达 | `/workflows/2068289624298184705` 可显示工作流名称、编码、节点、边、运行入口和运行记录 | PASS | 否 |
| M08-04 | 工作流编辑页加载持久化配置 | `/workflows/2068289624298184705/edit` 显示 code/name/Cron/调度状态和 2 条边路由 | PASS | 否 |
| M08-05 | 采集节点选中与绑定还原 | 编辑页默认选中采集节点，右侧显示 `长期回归-订单支付融合采集任务` | PASS | 否 |
| M08-06 | 质量节点选中与绑定还原 | 从画布选中质量节点后，右侧显示 `质量校验-样例表行数` 与绑定质量任务 | PASS | 否 |
| M08-07 | 数据脚本节点选中与绑定还原 | 从画布右侧可见区域选中脚本节点，右侧显示 `脚本查询-客户价值分层` 与绑定脚本 | PASS | 否 |
| M08-08 | 手动触发工作流 | 触发后生成运行 `2068295084980002817`，3 个节点均成功 | PASS | 否 |
| M08-09 | 工作流运行列表按工作流筛选 | `/runs?workflowDefinitionId=2068289624298184705` 稳定加载后显示 2 条运行，成功 2、成功节点 6 | PASS | 否 |
| M08-10 | 工作流运行列表按状态筛选 | `/runs?workflowDefinitionId=...&status=SUCCESS` 返回 2 条成功运行 | PASS | 否 |
| M08-11 | 工作流运行详情 | `/runs/2068295084980002817` 显示摘要、3 个节点、状态成功；成功运行不显示终止入口 | PASS | 否 |
| M08-12 | 节点日志 UI | 运行详情点击脚本节点“查看日志”，抽屉显示工作流、节点、Worker、日志大小 1.7 KB 和完整日志内容 | PASS | 否 |
| M08-13 | 节点日志接口 | 3 个节点的 `/runs/{runRecordId}/log` 与 `/download` 均返回成功日志 | PASS | 否 |
| M08-14 | 工作流重复编码负向校验 | 使用已存在 code 保存新工作流，接口返回 400：`Workflow code already exists in the current project` | PASS | 否 |
| M08-15 | 运维中心默认态 | `/ops-center` 显示 24 小时窗口整体告警、Worker 在线、队列为空；告警来自历史失败运行/服务/接入异常 | PASS | 否 |
| M08-16 | 运维中心筛选项 | 任务类型下拉含工作流节点/采集任务/质量任务；状态下拉含排队中/运行中/成功/失败/已停止；Worker 下拉含 `studio-online-worker-01` | PASS | 否 |
| M08-17 | 运维中心运行域筛选 | 筛选 `工作流节点 + 成功 + studio-online-worker-01` 后显示 6 条成功节点运行，服务/接入异常为空，整体健康为健康 | PASS | 已修复 |
| M08-18 | 控制台异常检查 | 未观察到 Studio error；仍有既有 Element Plus `ElPagination` 废弃 warning | PASS | 否，暂缓 |

## 关键接口证据

| 接口 | 结果摘要 |
|---|---|
| `GET /api/v1/workflows/2068289624298184705` | 返回工作流已发布、3 个节点、2 条边、调度关闭 |
| `POST /api/v1/workflows/2068289624298184705/trigger` | 手动触发成功 |
| `GET /api/v1/workflow-runs?workflowDefinitionId=2068289624298184705&pageNo=1&pageSize=5` | 最新运行 `2068295084980002817`，状态 `SUCCESS` |
| `GET /api/v1/workflow-runs/2068295084980002817` | 返回 3 个节点运行，均 `SUCCESS` |
| `GET /api/v1/runs/{runRecordId}/log` | 三个节点均可读取日志摘要 |
| `GET /api/v1/runs/{runRecordId}/log/download` | 三个节点均可下载完整日志 |
| `POST /api/v1/ops-center/overview/query` 默认 24 小时窗口 | `healthStatus=WARNING`、`failedRuns=2`、`serviceFailures=5`、`ingestionFailures=9`、`onlineWorkerInstances=1` |
| `POST /api/v1/ops-center/overview/query` 运行域筛选 | `healthStatus=HEALTHY`、`failedRuns=0`、`serviceFailures=0`、`ingestionFailures=0`、`runTotal=6` |
| `POST /api/v1/ops-center/runs/query` 运行域筛选 | `total=6`，均为工作流节点成功运行 |
| `POST /api/v1/ops-center/service-events/query` 运行域筛选 | `total=0` |
| `POST /api/v1/ops-center/ingestion-events/query` 运行域筛选 | `total=0` |

## 修复记录

| 编号 | 问题 | 影响 | 修复 | 回归 |
|---|---|---|---|---|
| FIX-M08-001 | 运维中心选择 `工作流节点 + 成功 + Worker` 后，运行表已正确筛选成功节点，但整体健康仍被不相关的数据服务/接入历史异常拉成告警 | 筛选语义不一致，容易误导用户认为当前工作流节点筛选结果存在服务/接入异常 | `OpsCenterService` 在运行域专属筛选条件 `executionType` 或 `workerGroupCode` 存在时隔离服务/接入事件；同时为服务/接入事件补充 `SUCCESS/FAILED` 状态过滤 | 单测、IDEA 编译、接口和 UI 均已通过 |

### 修改文件

| 文件 | 说明 |
|---|---|
| `backend/studio-infra/src/main/java/com/jdragon/studio/infra/service/OpsCenterService.java` | 收窄服务/接入日志查询的运行域筛选语义 |
| `backend/studio-test/src/test/java/com/jdragon/studio/test/OpsCenterServiceRegressionTest.java` | 新增运行域筛选隔离服务/接入事件的回归测试 |

### 验证命令

| 命令 | 结果 |
|---|---|
| `mvn -pl studio-test -am "-Dtest=OpsCenterServiceRegressionTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` | PASS，12 tests，0 failures，0 errors |
| IDEA build selected files | PASS，无问题 |
| 重启 IDEA `StudioServerApplication` | PASS，`NACOS_SERVER=127.0.0.1:8848`，Server health `UP` |
| 运维中心接口筛选回归 | PASS，运行域筛选后 `HEALTHY`，服务/接入事件 `0` |
| 运维中心 UI 筛选回归 | PASS，运行域筛选后整体健康显示健康，服务/接入异常表为空 |

## 观察与判定

| 编号 | 现象 | 复核结论 | 是否需要修复 |
|---|---|---|---|
| OBS-M08-001 | 工作流详情/运行详情首次快照可能显示加载中或空态 | 延长等待后页面稳定显示完整数据，判定为异步加载时机 | 否 |
| OBS-M08-002 | 工作流编辑画布第三个脚本节点右侧部分超出当前画布可视区域 | 节点左侧仍可见且可选中；不影响保存、执行和日志链路 | 否，后续 UI 体验可优化 |
| RISK-M08-001 | 控制台仍有 Element Plus `ElPagination` 废弃用法 warning | 与前序模块一致，不影响本轮主流程 | 暂缓 |

## 结论

M08 工作流调度与运维模块本轮通过。发现 1 个真实问题并已修复、回归通过。长期工作流、运行记录、节点日志和运维筛选证据全部保留，用于后续快速迭代回归。
