# 2026-06-22 S17 数据源当前表单 ACL 深挖执行记录

## 本轮范围

- 排除：不重复全覆盖、M00-M10、S01-S03 首轮、S04-S16 已闭环专项，只把 S01 作为 ACL 回归门槛复跑。
- 深挖：数据源当前表单连接测试 `POST /api/v1/datasources/test` 对 `request.id` 的项目边界、敏感字段保留逻辑和跨项目借用密码风险。
- 长期对象：长期项目 `长期回归测试项目` / `2068077680446365698`，接收项目 `长期回归-资源接收项目` / `2068301893337849857`，长期数据源 `长期回归-客户经营画像数据源` / `2068077811652583425`。
- 数据策略：未清理长期项目、长期库或历史对象；本轮仅新增 S17 脚本、测试记录和后端回归测试。

## 代码二次验证

- `DataSourceController#testCurrent` 直接调用 `DataSourceService.testConnection(DataSourceSaveRequest)`。
- 修复前 `DataSourceService.buildDefinitionForTest` 使用 `datasourceMapper.selectById(request.getId())` 读取旧数据源，用于 `preserveSensitiveValues` 保留加密敏感字段。
- 该路径没有调用 `ProjectResourceAccessService.assertWritable` 或 `assertReadable`，与保存态测试 `POST /datasources/{id}/test` 的项目隔离不一致。
- 影响：无权读取长期项目数据源的接收项目成员，只要构造同一个 `id`、提供非敏感连接字段并省略 `password`，即可复用源项目已加密密码完成连接测试。

## 执行结果

| 编号 | 模块 | 前置数据 | 步骤 | 预期 | 实际 | 结果 | 是否需要修复 | 证据 |
|---|---|---|---|---|---|---|---|---|
| S17-DS-001 | 数据源基线 | admin，长期项目 | 读取长期 MySQL 数据源 | 可读取 | `200 SUCCESS`，数据源 `长期回归-客户经营画像数据源` | PASS | 否 | S17 脚本 |
| S17-DS-002 | 项目隔离 | 接收项目成员 | 查询接收项目数据源列表 | 不包含长期项目私有数据源 | 仅可见共享 HTTP 数据源 `2068145017434324994` | PASS | 否 | S17 脚本 |
| S17-DS-003 | 项目隔离 | 接收项目成员 | `GET /datasources/2068077811652583425` | `404 NOT_FOUND` | `404 Datasource not found` | PASS | 否 | S17 脚本 |
| S17-DS-004 | 保存态连接测试 | 接收项目成员 | `POST /datasources/{id}/test` | `404 NOT_FOUND` | `404 Datasource not found` | PASS | 否 | S17 脚本 |
| S17-DS-005 | 模型发现 | 接收项目成员 | `POST /datasources/{id}/discover` | `404 NOT_FOUND` | `404 Datasource not found` | PASS | 否 | S17 脚本 |
| S17-DS-006 | 当前表单连接测试 | 接收项目成员，payload 省略 `password` | `POST /datasources/test`，body 带长期项目数据源 `id` 与非敏感连接字段 | `403 FORBIDDEN` | 修复前批次 `20260622033528` 返回 `200 SUCCESS` 且 `Connection success`；修复后最近通过批次 `20260622034448` 返回 `403 Resource belongs to another project` | PASS | 已修复 | `BUG-S17-001` |
| S17-DS-007 | 当前表单连接测试正向 | admin，长期项目，payload 省略 `password` | `POST /datasources/test` | 本项目编辑场景仍可保留敏感字段并测试成功 | 修复后返回 `200 SUCCESS`，`Connection success` | PASS | 否 | S17 脚本 |
| S17-REG-001 | 后端回归 | 本次代码 | 运行 `DataSourceApiRegressionTest`、`DataSourceServiceRegressionTest` | 全部通过 | 5 tests，0 failures，0 errors | PASS | 否 | Maven 输出 |
| S17-REG-002 | ACL 回归 | 长期 S01 脚本 | 运行 `studio_s01_permission_probe.py` | 25 PASS / 0 FAIL | 25 PASS / 0 FAIL / 0 BLOCKED | PASS | 否 | S01 脚本 |
| S17-UI-001 | Browser 复核 | admin，长期项目 | 打开 `/datasources` | 页面可达，长期数据源显示可用，无 console error/warn | 页面显示 `长期回归-客户经营画像数据源`、状态 `可用`，console error/warn 为空 | PASS | 否 | Browser |

## 修复

- 缺陷编号：`BUG-S17-001`
- 级别：高
- 根因：当前表单测试连接为了支持编辑时保留已加密密码，直接按 `request.id` 读取旧数据源，没有校验旧数据源是否属于当前项目。
- 修复文件：
  - `backend/studio-infra/src/main/java/com/jdragon/studio/infra/service/DataSourceService.java`
  - `backend/studio-test/src/test/java/com/jdragon/studio/test/DataSourceApiRegressionTest.java`
  - `backend/studio-test/src/test/java/com/jdragon/studio/test/DataSourceServiceRegressionTest.java`
  - `docs/测试/长期跟踪/scripts/studio_s17_datasource_current_form_acl_probe.py`
- 修复方式：`buildDefinitionForTest` 在 `request.id != null` 时改走 `requireWritableEntity(request.getId())`，复用已有项目写权限边界，只有本项目数据源编辑场景才允许保留敏感字段。

## 验证命令

```powershell
Set-Location C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\backend
$env:JAVA_HOME='C:\dev\Java\jdk-17.0.12'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; mvn -pl studio-test -am "-Dtest=DataSourceServiceRegressionTest,DataSourceApiRegressionTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
```

结果：2026-06-22 03:47 复跑通过，`Tests run: 5, Failures: 0, Errors: 0, Skipped: 0`。

```powershell
C:\dev\python-envs\studio-longterm-conda311\python.exe DataAggregation\data-aggregation-studio\docs\测试\长期跟踪\scripts\studio_s17_datasource_current_form_acl_probe.py
```

结果：修复前批次 `20260622033528` 为 `6 PASS / 1 FAIL / needFix 1`；重启 StudioServer 后修复批次 `20260622033749` 为 `7 PASS / 0 FAIL / 0 BLOCKED / needFix 0`；提交前最近通过批次 `20260622034448` 为 `7 PASS / 0 FAIL / 0 BLOCKED / needFix 0`。

```powershell
C:\dev\python-envs\studio-longterm-conda311\python.exe DataAggregation\data-aggregation-studio\docs\测试\长期跟踪\scripts\studio_s01_permission_probe.py
```

结果：2026-06-22 03:45 复跑通过，`25 PASS / 0 FAIL / 0 BLOCKED / needFix 0`。

## 环境

- Nacos readiness：`OK`
- Server health：`UP`
- Worker health：`UP`
- Server 已通过 IDEA `StudioServerApplication` 重启，使用 `NACOS_SERVER=127.0.0.1:8848`。

## 后续建议

- 继续沿用 S17 思路检查其他“当前表单/编辑态保留旧敏感字段”的接口，例如 HTTP、协议转换目标配置和接入服务调试模板。
- 对共享数据源的语义做专项：明确共享后是否允许连接测试、发现模型和派生模型；若允许，应补只读共享的显式测试矩阵。
