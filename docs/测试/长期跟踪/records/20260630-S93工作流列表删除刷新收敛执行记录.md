# 2026-06-30 S93 工作流列表删除刷新收敛执行记录

## 触发任务

延续 Studio 长期测试目标，继续从 build-nginx 前端可达页面出发，结合源码二次验证“表格接口只取展示字段、详情按 id 下钻、写操作刷新范围不可过大”的执行情况。本轮接续 S92 后的候选点，重点检查工作流列表。

## 长期约定

- Studio 入口：`http://127.0.0.1:8000/dfs/data-aggregation-studio`
- 默认账号：`admin / admin123`
- 长期项目：`长期回归测试项目` / `2068077680446365698`
- Nacos：`127.0.0.1:8848`
- 长期数据不清理，本轮未新增、未删除任何长期对象。
- 本轮 UI 复核只读访问工作流列表，不执行删除动作，避免破坏可追溯数据。

## 排查范围

| 候选点 | 页面/接口 | 结论 |
|---|---|---|
| 工作流列表 | `GET /api/v1/workflows/page` | 列表源头字段已在 S41 收敛，由 `WorkflowListSourceSlimmingRegressionTest` 覆盖；本轮复跑通过 |
| 工作流详情/编辑 | `GET /api/v1/workflows/{id}` | 详情页按 id 获取完整节点、边、版本和调度配置，符合下钻按需读取规则，未调整 |
| 工作流发布 | `POST /api/v1/workflows/{id}/publish` | 发布成功后已用 `patchWorkflowRow(...)` 局部更新当前行，未发现过度刷新 |
| 工作流触发 | `POST /api/v1/workflows/{id}/trigger` | 触发只显示结果提示，不刷新列表，符合刷新范围要求 |
| 工作流删除 | `DELETE /api/v1/workflows/{id}` | 删除成功后前端整页 `loadWorkflows()`，刷新范围超过当前行变化；本轮改为本地移除，只有当前页删空且仍有总数时才补拉 |
| 同类服务列表 | `/data-services`、`/data-ingestion-services`、`/protocol-conversions`、`/collection-tasks` | 已在 S89-S91 收敛为局部删除，本轮抽查未重复修复 |

## 问题确认

| 编号 | 模块 | 现象 | 真实存在 | 是否需要修复 |
|---|---|---|---|---|
| S93-WF-001 | 数据开发/工作流列表写后刷新 | `WorkflowsView.deleteWorkflow()` 删除成功后直接调用 `loadWorkflows()`，导致整个列表重新请求 | 是 | 是 |

根因：

- S41 已完成工作流列表源头字段瘦身，但删除动作仍保留旧的整页重拉逻辑。
- 删除只影响当前表格行和总数；除非当前页删空，否则没有必要重新请求当前页列表。

## 修复范围

| 文件 | 变更 |
|---|---|
| `frontend/apps/web/src/views/WorkflowsView.vue` | 删除成功后调用 `removeWorkflowRow(workflow)` 本地移除当前行并更新 `workflowTotal`；仅当前页删空且总数仍大于 0 时切到上一页并补拉 |

## 验证结果

| 用例 | 入口 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| S93-BE-001 | `WorkflowListSourceSlimmingRegressionTest` | 工作流列表和选项源头瘦身不回退 | 3 tests / 0 failures / 0 errors；Maven `BUILD SUCCESS` | PASS |
| S93-FE-001 | `npm run build:web` | 前端类型检查和生产构建成功 | `vue-tsc` 与 Vite build 成功，生成 `WorkflowsView-*.js` | PASS |
| S93-UI-001 | build-nginx `/workflows` | 工作流列表可达，长期项目上下文正确，无新增前端错误/警告 | 标题为“工作流编排”，上下文包含 `Default Tenant / 长期回归测试项目`，表格显示 17 行长期工作流，console warn/error 为 0 | PASS |
| S93-UI-002 | 删除刷新收敛 | 不破坏长期数据前提下验证删除逻辑已改为局部移除 | 源码中删除成功后调用 `removeWorkflowRow(workflow)`，仅在当前页空且总数仍大于 0 时调用 `loadWorkflows()`；本轮未执行真实删除 | PASS |

## 执行命令

```powershell
cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\backend
$env:JAVA_HOME='C:\dev\Java\jdk-17.0.12'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
C:\dev\apache-maven-3.8.8\bin\mvn.cmd -pl studio-test -am "-Dtest=WorkflowListSourceSlimmingRegressionTest" "-Dsurefire.failIfNoSpecifiedTests=false" test

cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\frontend
npm run build:web
```

## 结论

- 真实问题已修复。
- 工作流列表删除后的刷新范围已收敛，不再无条件重拉整个列表。
- 工作流列表源头字段仍由 S41 既有实现和 `WorkflowListSourceSlimmingRegressionTest` 保障，本轮复跑未回退。
- 工作流详情/编辑页仍按 id 获取完整定义，符合“下钻详情另取”的规则。
- 本轮没有新增、删除或清理长期数据；现有长期工作流数据继续保留，便于后续重测。
