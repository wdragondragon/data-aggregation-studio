# 2026-07-01 S104 工作流运行终止前置校验源头瘦身执行记录

## 触发任务

延续 Studio 长期测试目标，对上一轮工作流运行详情瘦身后的相邻链路继续结合代码二次验证。用户补充边界已纳入：分页列表和当前页状态刷新允许正常拉取，本轮不做极限优化，只处理隐藏详情读取、大 JSON 源头读取和跨上下文全局刷新。

## 长期约定

- Studio 入口：`http://127.0.0.1:8000/dfs/data-aggregation-studio`
- 默认账号：`admin / admin123`
- 长期项目：`长期回归测试项目` / `2068077680446365698`
- 长期库：`studio_longterm_regression`
- Nacos：`127.0.0.1:8848`
- 长期测试数据不清理。

## 本轮长期数据

本轮未新增长期测试数据，复用已有工作流运行：

| 类型 | 对象 | ID |
|---|---|---|
| 工作流运行 | `测试参数传递` | `2069333667478331393` |

## 排查范围

| 候选点 | 结论 |
|---|---|
| 工作流运行分页列表 | 属于当前页分页拉取，按用户边界不作为问题 |
| 工作流运行详情 | S101/S103 已修复运行记录和只读 DAG 源头瘦身，本轮回归保持通过 |
| 工作流运行终止 | 真实存在问题：`terminate` 写前调用完整 `get(workflowRunId)`，为了存在性校验额外读取一次运行详情 |
| 终止后返回详情 | 当前详情页需要刷新状态，保留为合理行为 |

## 问题确认

| 编号 | 模块 | 现象 | 真实存在 | 是否需要修复 |
|---|---|---|---|---|
| S104-WFRUN-001 | 工作流运行终止 | `POST /workflow-runs/{workflowRunId}/terminate` 在实际终止前调用 `get(workflowRunId)`，额外读取运行记录、调度任务、工作流节点和边 | 是 | 是 |

根因：

- 旧实现用 `get(workflowRunId)` 同时承担存在性/权限校验和详情返回。
- 写前校验只需要确认当前租户/项目下该 `workflowRunId` 是否存在于 `run_record` 或 `dispatch_task`。
- 终止后返回详情用于详情页刷新状态，属于页面当前上下文内的合理读取，不能一并移除。

## 修复范围

| 文件 | 变更 |
|---|---|
| `backend/studio-infra/src/main/java/com/jdragon/studio/infra/service/WorkflowRunService.java` | `terminate` 写前改为 `selectCount` 轻量存在性检查；保留终止后 `get` 返回详情 |
| `backend/studio-test/src/test/java/com/jdragon/studio/test/WorkflowRunSourceSlimmingRegressionTest.java` | 新增终止链路回归：写前只做 count，终止后只组装一次详情 |

## 验证结果

| 用例 | 入口 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| S104-BE-001 | `WorkflowRunSourceSlimmingRegressionTest` | 工作流运行列表、详情、终止链路源头瘦身回归通过 | 4 tests / 0 failures / 0 errors，Maven `BUILD SUCCESS` | PASS |
| S104-PKG-001 | `mvn -pl studio-server -am -DskipTests package` | Server exec jar 重新打包成功 | Reactor 7/7 SUCCESS，生成 `studio-server-0.1.0-SNAPSHOT-exec.jar` | PASS |
| S104-RUN-001 | 重启 `StudioServerApplication` 等价 Server | 新 `WorkflowRunService` 加载，Nacos 指向 `127.0.0.1:8848` | 新 PID `29368` 监听 `18080`，业务 API 可用；`/actuator/health` 仍为 `503/DOWN`，延续既有运行基线观察项 | PASS_WITH_OBSERVATION |
| S104-API-001 | build-nginx `GET /api/v1/workflow-runs/2069333667478331393` | 运行详情仍正常返回轻量 DAG | `success=true`，状态 `SUCCESS`，节点 2、边 1、节点运行 2，首节点 `config` 为空且 `fieldMappings` 为 0 | PASS |
| S104-API-002 | build-nginx `POST /api/v1/workflow-runs/999999999999999999/terminate` | 缺失运行返回 404/NOT_FOUND，不产生写入 | `HTTP 404`，`code=NOT_FOUND`，消息 `Workflow run not found: 999999999999999999` | PASS |
| S104-UI-001 | build-nginx `/runs/2069333667478331393` | 运行详情页面正常渲染，console 无 warn/error | 显示 `测试参数传递`，DAG 2 节点、1 条边、节点运行表 2 行，console warn/error `0` | PASS |

执行命令摘要：

```powershell
cd C:\dev\ideaProject\cloud-parent-springboot3\DataAggregation\data-aggregation-studio\backend
$env:JAVA_HOME='C:\dev\Java\jdk-17.0.12'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
mvn -pl studio-test -am '-Dtest=WorkflowRunSourceSlimmingRegressionTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
mvn -pl studio-server -am '-DskipTests' package
```

## 观察项

- 本轮不处理分页当前页重拉和详情页终止后刷新详情，这两类符合用户确认的测试边界。
- `/actuator/health` 仍返回 `503 {"status":"DOWN"}`，但登录、工作流运行详情 API 和 build-nginx 页面均正常；该观察项延续 S102/S103，不归因于 S104。
- 本轮最初用 JDK 8 跑 Maven 失败，改用 IDEA SDK 对应的 `C:\dev\Java\jdk-17.0.12` 后测试和打包均通过。

## 结论

- 真实问题已修复。
- 工作流运行终止写前不再组装完整运行详情，仅通过 `run_record` / `dispatch_task` count 做轻量存在性校验。
- 终止后仍返回详情给当前详情页刷新状态，符合“该拉取状态仍拉取”的边界。
- 后续涉及 `WorkflowRunService`、运行详情终止按钮、运行详情 API 或调度终止恢复服务时，至少复跑 `WorkflowRunSourceSlimmingRegressionTest`、build-nginx API `/workflow-runs/{workflowRunId}`、缺失运行 terminate API 和 build-nginx `/runs/{workflowRunId}`。
