# 2026-06-20 环境变更：Nacos 本地化执行记录

## 触发任务

远程 Nacos 突然不可访问，要求 Studio Server/Worker 改用 `127.0.0.1:8848`，并使用 IDEA 中的环境变量配置启动。

## 变更范围

| 项目 | 结果 |
|---|---|
| 本地 Nacos | `127.0.0.1:8848`，版本 `2.4.0`，状态可访问 |
| IDEA 配置 | `StudioServerApplication` 与 `StudioWorkerApplication` 的 `NACOS_SERVER` 均为 `127.0.0.1:8848` |
| Server 启动方式 | 已停止手工 `studio-server-*-exec.jar` 进程，改用 IDEA `StudioServerApplication` |
| Worker 启动方式 | 已停止手工 `studio-worker-*-exec.jar` 进程，改用 IDEA `StudioWorkerApplication` |
| 配置默认值 | 源码默认值保持仓库原有配置；本地 Nacos 切换仅通过 IDEA run configuration 中的 `NACOS_SERVER=127.0.0.1:8848` 覆盖 |

## IDEA 环境变量基线

| 应用 | 关键变量 |
|---|---|
| `StudioServerApplication` | `NACOS_SERVER=127.0.0.1:8848`、`REDIS_HOST=8.140.247.113`、`REDIS_PORT=9736`、`STUDIO_PYTHON_EXECUTABLE=C:\dev\python-envs\studio-longterm-conda311\python.exe`、`STUDIO_PYTHON_TEMP_DIR=C:\dev\python-envs\studio-longterm-conda311\tmp\server`、`STUDIO_RUN_LOG_STORAGE_TYPE=OBJECT_STORAGE` |
| `StudioWorkerApplication` | `NACOS_SERVER=127.0.0.1:8848`、`REDIS_HOST=8.140.247.113`、`REDIS_PORT=9736`、`STUDIO_PYTHON_EXECUTABLE=C:\dev\python-envs\studio-longterm-conda311\python.exe`、`STUDIO_PYTHON_TEMP_DIR=C:\dev\python-envs\studio-longterm-conda311\tmp\worker`、`STUDIO_RUN_LOG_STORAGE_TYPE=OBJECT_STORAGE` |

## 验证结果

| 编号 | 验证项 | 实际结果 | 结果 |
|---|---|---|---|
| ENV-NACOS-01 | 本地 Nacos 状态接口 | `/nacos/v1/console/server/state` 返回版本 `2.4.0`，端口 `8848` | PASS |
| ENV-NACOS-02 | Server 使用 IDEA run configuration 启动 | 进程命令行为 `com.jdragon.studio.server.bootstrap.StudioServerApplication`，包含 IntelliJ `idea_rt.jar` | PASS |
| ENV-NACOS-03 | Worker 使用 IDEA run configuration 启动 | 进程命令行为 `com.jdragon.studio.worker.bootstrap.StudioWorkerApplication`，包含 IntelliJ `idea_rt.jar` | PASS |
| ENV-NACOS-04 | Server/Worker Nacos 探测 | 启动日志均显示解析 `127.0.0.1:8848` 为可用 Nacos 服务 | PASS |
| ENV-NACOS-05 | 健康检查 | `http://127.0.0.1:18080/actuator/health` 与 `http://127.0.0.1:18081/actuator/health` 均返回 `UP` | PASS |
| ENV-NACOS-06 | Studio 登录接口 | `admin/admin123` 在长期项目上下文下登录成功，返回 token | PASS |

## 最新复核

2026-06-20 根据用户要求再次确认：后续 Studio Server/Worker 运行态以 IDEA run configuration 中的环境变量为准，不使用手工命令行临时覆盖。当前复核结果如下：

| 编号 | 复核项 | 实际结果 | 结果 |
|---|---|---|---|
| ENV-NACOS-07 | IDEA 环境变量 | `StudioServerApplication` 与 `StudioWorkerApplication` 均配置 `NACOS_SERVER=127.0.0.1:8848` | PASS |
| ENV-NACOS-08 | 进程来源 | Server/Worker 进程命令行均包含 IntelliJ `idea_rt.jar`，入口分别为 `StudioServerApplication`、`StudioWorkerApplication` | PASS |
| ENV-NACOS-09 | 本地 Nacos 可用性 | `http://127.0.0.1:8848/nacos/v1/console/server/state` 返回版本 `2.4.0`、端口 `8848` | PASS |
| ENV-NACOS-10 | Server/Worker 健康检查 | `18080` 与 `18081` actuator health 均返回 `UP` | PASS |
| ENV-NACOS-11 | Studio 登录接口 | `admin/admin123` 在长期项目 `2068077680446365698` 上下文下登录成功 | PASS |
| ENV-NACOS-12 | 源码配置边界 | 根模块 `application.yml` 未保留本地 Nacos 默认值改动；本地化只依赖 IDEA 环境变量 | PASS |

## 后续测试基线

后续 Studio 长期测试默认使用 IDEA run configuration 中的环境变量启动 Server/Worker，并以本地 `127.0.0.1:8848` Nacos 作为运行前置。长期项目、长期库和长期数据仍然保留，不做清理。
