# 数据开发模块 Web 冒烟结果

## 1. 结论

结论：通过。

本轮在真实 Web 前后端启动环境下完成了数据开发模块基础冒烟，已确认页面、上下文、空态与目录创建链路可用。

## 2. 执行环境

| 项目 | 信息 |
| --- | --- |
| 执行日期 | 2026-04-21 |
| 前端 | `http://127.0.0.1:5173` |
| Studio Server | `http://127.0.0.1:18080` |
| Studio Worker | `http://127.0.0.1:18081` |
| 数据库 | SQLite：`backend/studio-test/target/test-runtime/web-smoke.db` |
| 初始化方式 | `StudioDataInitializerApplication --studio.init.reset=true` |
| 登录账号 | `admin / admin123` |
| 当前租户 / 项目 | `Default Tenant / Default Project` |
| 本轮目录样例 | `ods-web-smoke` |
| 本轮权限编码样例 | `tenant:data-dev:ods:web-smoke` |

## 3. 执行结果

| 用例 | 验证点 | 结果 |
| --- | --- | --- |
| DD-UI-001 | 登录后进入 `/data-development`，页头显示 `Default Tenant / Default Project` | 通过 |
| DD-UI-002 | 初始化空项目下，目录树与编辑区空态文案正常展示 | 通过 |
| DD-UI-003 | Web 端新建目录并保存，页面出现“目录已保存”，树节点新增 `ods-web-smoke` | 通过 |
| DD-UI-004 | 点击“新建脚本”后，文件名、脚本类型、数据源、描述、编辑器和执行结果区均可见 | 通过 |
| DD-UI-005 | 本轮切页与操作后，浏览器控制台未发现 `error` / `warn` | 通过 |

## 4. 现场记录

目录创建成功后，左侧树结构出现：

| 节点 | 状态 |
| --- | --- |
| `ods-web-smoke` | 已显示 |

新建脚本页面可见以下关键区域：

| 区域 | 状态 |
| --- | --- |
| 文件名 | 已显示 |
| 脚本类型 | 已显示，默认 `SQL` |
| 数据源选择 | 已显示 |
| 脚本描述 | 已显示 |
| Monaco 编辑器 | 已显示 |
| 执行结果区域 | 已显示 |

## 5. 本轮未覆盖

以下能力本轮未在 Web 页面上继续深测：

| 项目 | 说明 |
| --- | --- |
| 脚本保存 | 本轮只验证了“新建脚本”表单加载，未继续保存 SQL / JAVA / PYTHON 脚本 |
| 脚本执行 | 本轮未在 Web 页面触发实际执行；脚本执行能力已由后端回归测试覆盖 |
| 数据源联动提示 | 本轮未配置数据库数据源，不涉及 SQL 智能提示的联调 |

## 6. 说明

本轮目标是确认 JDK17 + Spring Boot 3 迁移后，Web 端数据开发页面的基础交互没有被破坏。深度脚本执行链路仍建议与专项回归联动验证。
