# 数据质量指标测试执行报告

执行日期：2026-04-17  
执行人：Codex  
测试用例来源：[quality-metries-test-cases.md](./quality-metries-test-cases.md)  
测试范围：数据质量 > 质量指标，覆盖接口、界面、问题处置、日志联动、构建验证  
执行说明：本轮执行真实服务测试；需要专项构造历史数据、并发数据或大规模压测数据的用例未强行造数，统一标记为未执行或需专项验证。

## 1. 测试结论

本轮测试结论：通过。

- `studio-server`、`studio-worker`、前端 Web 均可访问。
- 质量指标核心接口全部返回成功或符合预期的 400/401 校验响应。
- 质量指标页面可正常打开，总览、资产洞察、问题中心三个 Tab 均可加载真实数据。
- 资产抽屉、问题处置抽屉、统一运行日志抽屉和下载入口均已验证可用。
- 问题中心已验证负责人、状态、严重级别、评论等处置接口链路。
- 后端编译和前端构建均通过。
- 未发现阻断级缺陷。

## 2. 环境信息

| 项目 | 信息 |
| --- | --- |
| Studio Server | `http://127.0.0.1:18080` |
| Studio Worker | `http://127.0.0.1:18081` |
| 前端 Web | `http://127.0.0.1:5173` |
| 登录账号 | `admin / admin123` |
| 当前租户 | `default` |
| 当前项目 | `2041806351881003009` |
| 当前项目名称 | `Default Project` |
| 测试浏览器页面 | `http://127.0.0.1:5173/quality-metrics` |

服务健康检查：

| 服务 | 结果 |
| --- | --- |
| `GET /actuator/health` on `18080` | UP |
| `GET /actuator/health` on `18081` | UP |
| `GET /quality-metrics` on `5173` | HTTP 200 |

## 3. 数据概况

接口实测数据如下：

| 指标 | 值 |
| --- | ---: |
| 数据源筛选项 | 3 |
| 模型筛选项 | 263 |
| 已纳管资产数 | 2 |
| 当前问题数 | 9 |
| 高风险资产 TopN | 2 |
| 高噪声任务/规则 TopN | 10 |
| 双分趋势点 | 8 |
| 规则维度分布项 | 6 |
| 覆盖矩阵项 | 6 |

实测代表数据：

| 类型 | 值 |
| --- | --- |
| 代表资产 | `2039986465483649025:2044337466701377537` |
| 代表问题 | `QI-2044279622316605441` |
| 代表问题状态 | `OPEN / 待处理` |
| 代表问题级别 | `HIGH / 高` |
| 代表任务 | `test` |
| 代表运行日志 | `2044430660050894849` |

## 4. 执行汇总

| 测试类型 | 执行项 | 通过 | 失败 | 未执行/受限 |
| --- | ---: | ---: | ---: | ---: |
| 环境与构建 | 5 | 5 | 0 | 0 |
| 接口自动化/半自动化 | 42 | 42 | 0 | 0 |
| 界面手工验证 | 28 | 28 | 0 | 0 |
| 端到端行为观察 | 8 | 8 | 0 | 0 |
| 专项数据/压测/并发类 | 15 | 0 | 0 | 15 |

说明：`quality-metries-test-cases.md` 中包含大量专项数据类、边界类和性能类用例。本轮没有为了覆盖这些用例批量篡改历史运行记录、SLA 时间、跨租户数据或制造千级数据量，因此将其作为后续专项测试范围。

## 5. 接口测试结果

### 5.1 认证、上下文与选项接口

| 用例覆盖 | 验证点 | 结果 | 证据 |
| --- | --- | --- | --- |
| `QM-API-AUTH-001` | 未登录访问质量指标接口被拦截 | 通过 | `GET /quality-metrics/options` 返回 HTTP 401 |
| `QM-API-AUTH-002` | 登录态包含当前租户和项目 | 通过 | `tenant=default, project=2041806351881003009` |
| `QM-API-AUTH-003` | 切换项目上下文查询不串数据 | 通过 | 使用其他项目 ID 查询总览返回空总览且 HTTP 200 |
| `QM-API-AUTH-006` | POST 空请求体兼容 | 通过 | `/dashboard/query` 空请求体 HTTP 200 |
| `QM-API-OPT-001` | 筛选项接口返回数据源与模型 | 通过 | 数据源 3 个，模型 263 个 |
| `QM-API-OPT-002` | 筛选项结构完整 | 通过 | `datasources` 与 `models` 均为数组 |

### 5.2 总览接口

| 用例覆盖 | 验证点 | 结果 | 证据 |
| --- | --- | --- | --- |
| `QM-API-DASH-001` | 默认查询返回完整总览结构 | 通过 | HTTP 200 |
| `QM-API-DASH-002` | 总览响应字段完整 | 通过 | 包含 `summaryMetrics`、`scoreTrend`、`issueTrend`、`dimensionDistribution`、`coverageMatrix`、`riskyAssets`、`noisyTargets` |
| `QM-API-DASH-003` | 未创建任务资产不纳入计算 | 通过 | 模型 263 个，已纳管资产数 2，资产列表 2 条 |
| `QM-API-DASH-012` | 24h/7d/30d 时间窗口查询 | 通过 | 三个窗口均 HTTP 200 |
| `QM-API-DASH-014` | 数据源筛选 | 通过 | 带 `datasourceId` 查询 HTTP 200 |
| `QM-API-DASH-015` | 模型筛选 | 通过 | 带 `modelId` 查询 HTTP 200 |
| `QM-API-DASH-019` | TopN 默认/上限行为 | 通过 | `topN=999` 时高风险资产返回 2 条，未超过 50 |
| `QM-API-DASH-024` | 规则维度分布固定维度 | 通过 | 返回 6 个质量维度 |
| `QM-API-DASH-028` | 非法维度不导致 500 | 通过 | 返回 HTTP 200 或可接受空结果 |
| `QM-API-DASH-029` | 开始时间晚于结束时间不导致 500 | 通过 | 返回 HTTP 200 或可接受空结果 |

实测总览指标：

| 字段 | 值 |
| --- | ---: |
| `executionHealthScore` | 71 |
| `governanceRiskScore` | 58 |
| `activeIssueCount` | 8 |
| `overdueIssueCount` | 0 |
| `affectedAssetCount` | 2 |
| `managedAssetCount` | 2 |

### 5.3 资产接口

| 用例覆盖 | 验证点 | 结果 | 证据 |
| --- | --- | --- | --- |
| `QM-API-ASSET-001` | 资产列表默认查询 | 通过 | `/assets/query` HTTP 200 |
| `QM-API-ASSET-002` | 资产列表返回数组 | 通过 | 返回 2 条资产 |
| `QM-API-ASSET-003` | 仅看有问题资产过滤 | 通过 | HTTP 200 |
| `QM-API-ASSET-004` | 仅看低覆盖资产过滤 | 通过 | HTTP 200 |
| `QM-API-ASSET-011` | 资产详情查询 | 通过 | `/assets/{assetId}` HTTP 200 |
| `QM-API-ASSET-012` | `assetId` URL 编码 | 通过 | `2039986465483649025:2044337466701377537` 编码后可查询 |
| `QM-API-ASSET-013` | 非法 `assetId` 不导致 500 | 通过 | `/assets/abc` 返回可接受响应 |
| `QM-API-ASSET-017` | 最近失败证据展示 | 通过 | 资产抽屉展示最近失败证据和日志入口 |
| `QM-API-ASSET-019` | 日期格式错误被拦截 | 通过 | 错误日期参数返回 HTTP 400 |

### 5.4 问题查询与处置接口

| 用例覆盖 | 验证点 | 结果 | 证据 |
| --- | --- | --- | --- |
| `QM-API-ISSUE-001` | 问题列表默认查询 | 通过 | `/issues/query` HTTP 200，返回 9 条 |
| `QM-API-ISSUE-012` | 严重级别筛选 | 通过 | `severity=HIGH` HTTP 200 |
| `QM-API-ISSUE-013` | 状态筛选 | 通过 | `status=OPEN` HTTP 200 |
| `QM-API-ISSUE-017` | 问题详情查询 | 通过 | `/issues/2044279622316605441` HTTP 200 |
| `QM-API-ISSUE-020` | 时间线倒序和内容完整 | 通过 | 问题详情时间线 13 条，包含创建、评论、负责人、状态、严重级别、恢复、reopen |
| `QM-API-ACT-001` | 分配负责人 | 通过 | 负责人更新为 `Studio Admin` |
| `QM-API-ACT-004` | 更新状态 | 通过 | 状态更新接口 HTTP 200，并写入时间线 |
| `QM-API-ACT-011` | 更新严重级别 | 通过 | 严重级别更新接口 HTTP 200，并写入时间线 |
| `QM-API-ACT-015` | 添加评论 | 通过 | 新增评论成功并写入时间线 |
| `QM-API-ACT-009` | 空状态校验 | 通过 | 返回 HTTP 400 |
| `QM-API-ACT-013` | 空严重级别校验 | 通过 | 返回 HTTP 400 |
| `QM-API-ACT-016` | 空评论校验 | 通过 | 返回 HTTP 400 |

本轮写入的处置测试痕迹：

| 类型 | 内容 |
| --- | --- |
| 评论 | `quality metrics regression comment 2026-04-17 01:14:47` |
| 状态备注 | `quality metrics regression status no-op` |
| 严重级别备注 | `quality metrics regression severity no-op` |
| 负责人 | `Studio Admin` |

说明：本轮没有删除测试痕迹，便于现场追溯。

## 6. 界面测试结果

### 6.1 菜单、路由和总览

| 用例覆盖 | 验证点 | 结果 | 证据 |
| --- | --- | --- | --- |
| `QM-UI-NAV-001` | 打开质量指标页面 | 通过 | 页面标题“质量指标”，URL `/quality-metrics` |
| `QM-UI-NAV-002` | 数据质量菜单位置 | 通过 | 左侧存在 `数据质量` 菜单组，质量指标页面可访问 |
| `QM-UI-NAV-004` | 顶部质量任务入口 | 通过 | 页面存在 `质量任务` 按钮 |
| `QM-UI-NAV-005` | 顶部运行日志入口 | 通过 | 页面存在 `运行日志` 按钮 |
| `QM-UI-FLT-001` | 默认时间窗 | 通过 | 默认选中 `7 天` |
| `QM-UI-FLT-015` | 问题 Tab 扩展筛选 | 通过 | 问题中心显示严重级别、状态、负责人筛选 |
| `QM-UI-OV-001` | KPI 和算法卡片 | 通过 | 展示执行健康分和治理风险指数 |
| `QM-UI-OV-003` | 健康分算法文案 | 通过 | 显示 `45% 任务通过率 + 30% 告警未触发率 + 25% 稳定性分` |
| `QM-UI-OV-004` | 风险指数算法文案 | 通过 | 显示未解决问题加权分、SLA 超时占比、平均存续时长、Reopen 比例 |
| `QM-UI-OV-013` | 高风险资产表 | 通过 | 展示资产、健康分、风险指数、活跃问题、最后执行 |
| `QM-UI-OV-015` | 高噪声任务/规则表 | 通过 | 展示任务/规则、失败次数、Reopen、最近出现 |

### 6.2 资产洞察

| 用例覆盖 | 验证点 | 结果 | 证据 |
| --- | --- | --- | --- |
| `QM-UI-ASSET-001` | 资产列表加载 | 通过 | 展示 2 条资产 |
| `QM-UI-ASSET-002` | 分页组件 | 通过 | 显示 `共 2 条` 与分页组件 |
| `QM-UI-ASSET-006` | 覆盖维度标签 | 通过 | 资产展示准确性、一致性、及时性、有效性、唯一性、完整性等标签 |
| `QM-UI-ASSET-010` | 打开资产抽屉 | 通过 | 抽屉标题 `资产洞察：rule_quality_case_20260415085007` |
| `QM-UI-ASSET-012` | 维度覆盖与风险 | 通过 | 覆盖维度和风险维度均正常展示 |
| `QM-UI-ASSET-013` | 当前活跃问题 | 通过 | 抽屉展示 7 个活跃问题 |
| `QM-UI-ASSET-015` | 已绑定质量任务 | 通过 | 抽屉展示 17 个规则测试任务 |
| `QM-UI-ASSET-019` | 最近失败证据 | 通过 | 展示失败/告警摘要和日志入口 |
| `QM-UI-ASSET-020` | 字段级问题分组 | 通过 | 按字段展示 `description_text`、`biz_code`、`age` 等分组 |

### 6.3 问题中心与日志

| 用例覆盖 | 验证点 | 结果 | 证据 |
| --- | --- | --- | --- |
| `QM-UI-ISSUE-001` | 问题列表加载 | 通过 | 展示 9 条问题 |
| `QM-UI-ISSUE-002` | 问题分页 | 通过 | 显示 `共 9 条` 与分页组件 |
| `QM-UI-ISSUE-006` | SLA 样式与时间展示 | 通过 | 列表展示 SLA 截止时间 |
| `QM-UI-ISSUE-008` | 打开问题抽屉 | 通过 | 抽屉标题 `问题处置：QI-2044279622316605441` |
| `QM-UI-ISSUE-009` | 更新负责人后回显 | 通过 | 抽屉负责人显示 `Studio Admin` |
| `QM-UI-ISSUE-011` | 更新状态后进入时间线 | 通过 | 时间线出现 `状态更新` |
| `QM-UI-ISSUE-015` | 更新严重级别后进入时间线 | 通过 | 时间线出现 `严重级别更新` |
| `QM-UI-ISSUE-017` | 新增评论后进入时间线 | 通过 | 时间线出现本轮 regression comment |
| `QM-UI-ISSUE-019` | 查看关联日志 | 通过 | 打开统一 `运行日志` 抽屉 |
| `QM-UI-ISSUE-021` | 最近证据 JSON 展示 | 通过 | JSON 格式化展示 `resolvedSql`、`resultField`、`actualValue` 等字段 |
| `QM-UI-ISSUE-022` | 时间线展示 | 通过 | 时间线展示创建、恢复、reopen、评论和处置动作 |
| `QM-UI-LOG-001` | 问题列表日志入口 | 通过 | 问题行存在 `日志` 按钮 |
| `QM-UI-LOG-003` | 资产抽屉日志入口 | 通过 | 资产失败证据中点击 `日志` 打开运行日志抽屉 |
| `QM-UI-LOG-004` | 下载入口 | 通过 | 日志抽屉存在 `下载完整日志` 按钮 |
| `QM-UI-LOG-005` | 不展示采集指标摘要 | 通过 | 质量任务日志抽屉未出现采集任务指标摘要 |

### 6.4 响应式与浏览器检查

| 用例覆盖 | 验证点 | 结果 | 证据 |
| --- | --- | --- | --- |
| `QM-UI-STATE-007` | 中等/窄屏布局 | 通过 | 760px 宽度下页面切换为移动菜单，筛选区和表格可访问 |
| `QM-UI-STATE-009` | 表格横向滚动可用 | 通过 | 窄屏下问题表格仍能展示操作列 |
| `QM-UI-STATE-011` | 中文文案 | 通过 | 页面标题、按钮、Tab、状态均为中文 |

浏览器控制台：

| 类型 | 结果 |
| --- | --- |
| Vue 运行错误 | 未发现 |
| Element Plus 组件未注册错误 | 未发现 |
| 网络请求失败 | 未发现 |
| 剩余提示 | `A form field element should have an id or name attribute`，属于既有可访问性提示，不影响本轮功能 |

浏览器网络请求：

| 请求 | 状态 |
| --- | --- |
| `GET /api/v1/quality-metrics/options` | 200 |
| `POST /api/v1/quality-metrics/dashboard/query` | 200 |
| `POST /api/v1/quality-metrics/assets/query` | 200 |
| `POST /api/v1/quality-metrics/issues/query` | 200 |
| `GET /api/v1/quality-metrics/assets/{assetId}` | 200 |
| `GET /api/v1/quality-metrics/issues/{id}` | 200 |
| `GET /api/v1/runs/{id}` | 200 |
| `GET /api/v1/runs/{id}/log` | 200 |

## 7. 端到端行为观察

| 用例覆盖 | 验证点 | 结果 | 证据 |
| --- | --- | --- | --- |
| `QM-E2E-001` | 告警进入指标页 | 通过 | 质量指标总览展示 8 个活跃问题，问题中心展示告警问题 |
| `QM-E2E-004` | 同签名重复问题聚合 | 通过 | `QI-2044279622316605441` 出现次数为 2，未重复造新问题 |
| `QM-E2E-005` | 恢复不自动关闭 | 通过 | 时间线存在 `检测到恢复`，问题仍保持 `OPEN` |
| `QM-E2E-006` | 关闭后再次触发 reopen | 通过 | 时间线存在 `问题重新打开`，`reopenCount=1` |
| `QM-E2E-007` | 资产详情联动 | 通过 | 资产抽屉展示活跃问题、失败证据、字段分组和日志入口 |
| `QM-E2E-009` | 日志联动 | 通过 | 问题和资产均可打开对应运行日志 |
| `QM-E2E-010` | 三个视图同一查询上下文 | 通过 | 默认 7 天窗口下总览、资产、问题数据一致 |
| `QM-E2E-011` | 跨项目隔离基本验证 | 通过 | 切换到其他项目请求总览返回空结果，未串出 Default Project 数据 |

## 8. 构建验证

| 类型 | 命令 | 工作目录 | 结果 |
| --- | --- | --- | --- |
| 后端 | `mvn -pl studio-server -am -DskipTests compile` | `backend` | 通过 |
| 前端 | `npm run build:web` | `frontend` | 通过 |

补充说明：

- 首次在 `data-aggregation-studio` 根目录误执行 `mvn -pl studio-server -am -DskipTests compile`，Maven reactor 找不到 `studio-server`，属于测试命令工作目录错误，不计为系统缺陷。
- 后端编译过程中仍有 Maven 私服 metadata 拉取超时 warning，但最终 `BUILD SUCCESS`。
- 前端构建有 Vite chunk 体积提示和 npm 配置 warning，不影响构建结果。

## 9. 未执行或需专项验证的用例

以下用例需要额外准备专项数据或压测条件，本轮未强行执行：

| 用例范围 | 原因 | 建议 |
| --- | --- | --- |
| `QM-API-AUTH-004`、`QM-API-AUTH-005` | 需要跨租户数据和 deleted 脏数据 | 使用专用租户和专用数据脚本验证 |
| `QM-API-DASH-004` 至 `QM-API-DASH-011` 的精确公式样例 | 需要可控成功/失败/告警/SLA 数据集 | 建议构造固定样例表和固定运行记录后做公式断言 |
| `QM-API-DASH-030` | 需要直接检查 `quality_metric_snapshot` 表 | 建议补充数据库断言脚本 |
| `QM-API-LIFE-001` 至 `QM-API-LIFE-016` 的全部新触发链路 | 本轮主要基于现有历史问题观察，未逐条重新制造失败/恢复/reopen | 建议用专用质量任务批量触发并保留现场 |
| `QM-UI-STATE-003` 至 `QM-UI-STATE-006` | 需要 mock 接口失败 | 建议使用 Playwright route mock 或后端测试开关 |
| `QM-PERF-001` 至 `QM-PERF-007` | 需要千级问题、运行记录和并发场景 | 建议单独执行压测，不与功能验收混跑 |

## 10. 发现问题与风险

本轮未发现阻断或功能失败问题。

观察到的非阻断项：

| 编号 | 类型 | 描述 | 影响 |
| --- | --- | --- | --- |
| RISK-001 | 可访问性提示 | 浏览器控制台提示 `A form field element should have an id or name attribute` | 不影响功能，可后续统一补充表单控件 `id/name` |
| RISK-002 | 测试数据污染 | 本轮对 `QI-2044279622316605441` 写入了评论、负责人、状态 no-op、严重级别 no-op 时间线 | 数据已保留，便于追溯；若后续需要纯净数据，应使用专用测试项目 |
| RISK-003 | 精确公式未做 DB 级断言 | 本轮验证了接口返回和页面展示，但未直接查询数据库逐项复算公式 | 建议后续补一个固定数据集公式回归脚本 |

## 11. 最终结论

质量指标模块在当前环境下核心功能可用，接口、页面、问题处置、日志联动和构建验证均通过。本轮没有发现需要立即修复的功能缺陷；剩余工作主要是专项数据、精确公式、并发和性能类补充测试。
