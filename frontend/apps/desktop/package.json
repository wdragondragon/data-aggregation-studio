{
  "name": "@studio/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src-electron/main.cjs",
  "scripts": {
    "dev": "concurrently -k \"vite --host localhost --port 5174\" \"wait-on http-get://localhost:5174 && electron . --dev\"",
    "build": "vue-tsc --noEmit && vite build",
    "start": "electron ."
  },
  "dependencies": {
    "@studio/api-sdk": "0.1.0",
    "@studio/i18n": "0.1.0",
    "@studio/meta-form": "0.1.0",
    "@studio/ui": "0.1.0",
    "@studio/workflow-designer": "0.1.0",
    "vue-i18n": "^9.14.4"
  }
}
